---
phase: 02-tabular-plugin-experiment-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mlforge/tabular/__init__.py
  - src/mlforge/tabular/prepare.py
  - src/mlforge/tabular/baselines.py
  - src/mlforge/templates/tabular_train.py.j2
  - tests/mlforge/test_tabular.py
  - tests/mlforge/test_baselines.py
autonomous: true
requirements: [TABL-01, TABL-02, TABL-03, TABL-04, TABL-05, INTL-01, INTL-02]

must_haves:
  truths:
    - "TabularPlugin satisfies DomainPlugin Protocol via isinstance check"
    - "TabularPlugin.scaffold() creates frozen prepare.py and mutable train.py in target directory"
    - "TabularPlugin.template_context() returns domain_rules list with tabular ML rules including dual-baseline gate"
    - "TabularPlugin.validate_config() rejects unknown metrics and accepts valid ones"
    - "prepare.py loads CSV and Parquet files, splits data, builds sklearn preprocessor, and evaluates models"
    - "Baseline computation produces naive and domain-specific baseline scores using sklearn DummyClassifier/DummyRegressor"
    - "Dual-baseline gate rejects metric values that do not beat ALL baselines"
    - "train.py template includes Optuna boilerplate with sklearn/XGBoost/LightGBM support"
    - "Leakage prevention utilities exist for temporal data: shift-first features and walk-forward CV"
  artifacts:
    - path: "src/mlforge/tabular/__init__.py"
      provides: "TabularPlugin class implementing DomainPlugin Protocol"
      exports: ["TabularPlugin"]
    - path: "src/mlforge/tabular/prepare.py"
      provides: "Frozen data pipeline: load_data, split_data, build_preprocessor, evaluate, get_data_summary"
      min_lines: 80
    - path: "src/mlforge/tabular/baselines.py"
      provides: "Baseline computation and dual-baseline gate"
      exports: ["compute_baselines", "passes_baseline_gate"]
    - path: "src/mlforge/templates/tabular_train.py.j2"
      provides: "Jinja2 template for mutable train.py with Optuna + multi-family support"
      min_lines: 30
    - path: "tests/mlforge/test_tabular.py"
      provides: "Tests for TabularPlugin scaffold, template_context, validate_config, and temporal utils"
      min_lines: 60
    - path: "tests/mlforge/test_baselines.py"
      provides: "Tests for baseline computation and dual-baseline gate"
      min_lines: 40
  key_links:
    - from: "src/mlforge/tabular/__init__.py"
      to: "src/mlforge/plugins.py"
      via: "DomainPlugin Protocol conformance"
      pattern: "isinstance.*DomainPlugin"
    - from: "src/mlforge/tabular/__init__.py"
      to: "src/mlforge/templates/__init__.py"
      via: "get_template_env for train.py rendering"
      pattern: "get_template_env"
    - from: "src/mlforge/tabular/baselines.py"
      to: "sklearn.dummy"
      via: "DummyClassifier/DummyRegressor for baseline models"
      pattern: "DummyClassifier|DummyRegressor"
---

<objective>
Implement the TabularPlugin that proves the Phase 1 plugin architecture works for real ML tasks, along with baseline computation, dual-baseline gate, and CLAUDE.md protocol generation for tabular ML.

Purpose: This is the first concrete domain plugin -- it validates that the DomainPlugin Protocol, template rendering, and hook engine work end-to-end for classification and regression on CSV/Parquet data.
Output: TabularPlugin class, frozen prepare.py, train.py Jinja2 template, baselines module, and full test coverage.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tabular-plugin-experiment-intelligence/02-RESEARCH.md

<interfaces>
<!-- Phase 1 interfaces this plan builds on -->

From src/mlforge/plugins.py:
```python
@runtime_checkable
class DomainPlugin(Protocol):
    name: str
    frozen_files: list[str]
    def scaffold(self, target_dir: Path, config: Config) -> None: ...
    def template_context(self, config: Config) -> dict: ...
    def validate_config(self, config: Config) -> list[str]: ...

def register_plugin(plugin: DomainPlugin) -> None: ...
def get_plugin(name: str) -> DomainPlugin: ...
```

From src/mlforge/config.py:
```python
@dataclass
class Config:
    domain: str = "tabular"
    metric: str = "accuracy"
    direction: str = "maximize"
    budget_minutes: int = 60
    budget_experiments: int = 50
    frozen_files: list[str] = field(default_factory=lambda: ["prepare.py"])
    mutable_files: list[str] = field(default_factory=lambda: ["train.py"])
    plugin_settings: dict = field(default_factory=dict)
```

From src/mlforge/templates/__init__.py:
```python
def get_template_env() -> Environment: ...
def render_claude_md(plugin: DomainPlugin, config: Config) -> str: ...
def render_experiments_md(config: Config, run_id: str) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TabularPlugin + prepare.py + baselines + train.py template</name>
  <files>
    pyproject.toml,
    src/mlforge/tabular/__init__.py,
    src/mlforge/tabular/prepare.py,
    src/mlforge/tabular/baselines.py,
    src/mlforge/templates/tabular_train.py.j2,
    tests/mlforge/test_tabular.py,
    tests/mlforge/test_baselines.py
  </files>
  <behavior>
    - TabularPlugin satisfies DomainPlugin Protocol: isinstance(TabularPlugin(), DomainPlugin) is True
    - TabularPlugin.name == "tabular" and frozen_files == ["prepare.py"]
    - scaffold() writes prepare.py (copied from tabular/prepare.py source) and train.py (rendered from template) to target_dir
    - scaffold() target_dir contains both files after call
    - template_context() returns dict with "domain_rules" (list of strings) and "extra_sections" (list)
    - template_context() includes rules about dual-baseline gate, frozen prepare.py, sklearn estimators
    - template_context() includes classification-specific rule (StratifiedKFold) when task=classification
    - template_context() includes regression-specific rule (KFold) when task=regression
    - validate_config() returns empty list for valid metrics (accuracy, rmse, r2, f1, etc.)
    - validate_config() returns error message for unknown metrics
    - prepare.py load_data() reads CSV files via pd.read_csv
    - prepare.py load_data() reads Parquet files via pd.read_parquet (detected by .parquet/.pq suffix)
    - prepare.py split_data() returns X_train, X_test, y_train, y_test with configurable test_size
    - prepare.py build_preprocessor() returns ColumnTransformer handling numeric (impute+scale) and categorical (impute+encode) columns
    - prepare.py evaluate() scores a model using cross_val_score with appropriate CV (StratifiedKFold for classification, KFold for regression)
    - prepare.py get_data_summary() returns dict with shape, dtypes, target_distribution info
    - prepare.py has temporal_split() for walk-forward CV splitting (respects time ordering, no shuffle)
    - prepare.py has validate_no_leakage() that checks shift-first pattern (lagged features only, no future data)
    - compute_baselines() returns dict mapping strategy name to dict with score and std
    - compute_baselines() uses DummyClassifier(most_frequent, stratified) for classification
    - compute_baselines() uses DummyRegressor(mean, median) for regression
    - passes_baseline_gate() returns True only when metric beats ALL baselines
    - passes_baseline_gate() handles both maximize and minimize directions
    - train.py template renders valid Python with Optuna study, sklearn/XGBoost/LightGBM imports
  </behavior>
  <action>
    1. Add ML dependencies to pyproject.toml: scikit-learn>=1.5, pandas>=2.0, numpy>=2.0, xgboost>=2.0, lightgbm>=4.0, optuna>=4.0, pyarrow>=15.0

    2. Create src/mlforge/tabular/__init__.py with TabularPlugin class:
       - name = "tabular", frozen_files = ["prepare.py"]
       - scaffold(): copies prepare.py from mlforge.tabular.prepare source module and renders train.py from tabular_train.py.j2 template using config values (csv_path, target_column, metric, time_budget from plugin_settings)
       - template_context(): returns domain_rules list with tabular ML rules (dual-baseline gate, frozen prepare.py, sklearn compatibility, metric direction, task-specific CV strategy) and extra_sections list
       - validate_config(): validates metric against known set {accuracy, auc, roc_auc, f1, f1_weighted, precision, recall, log_loss, rmse, mae, r2, mse}
       - Guard ML imports: use try/except ImportError at module level or lazy imports so core mlforge can import without ML deps

    3. Create src/mlforge/tabular/prepare.py -- frozen data pipeline module:
       - load_data(path: Path) -> pd.DataFrame: detect .parquet/.pq suffix for read_parquet, else read_csv
       - split_data(df, target_column, test_size=0.2, random_state=42) -> tuple[X_train, X_test, y_train, y_test]: uses train_test_split
       - build_preprocessor(X: pd.DataFrame) -> ColumnTransformer: auto-detect numeric vs categorical columns, numeric gets SimpleImputer(median)+StandardScaler, categorical gets SimpleImputer(most_frequent)+OneHotEncoder(handle_unknown='ignore')
       - evaluate(model, X, y, scoring, task, cv_splits=5) -> dict with mean and std: uses cross_val_score with StratifiedKFold (classification) or KFold (regression)
       - get_data_summary(df, target_column) -> dict: shape, feature_types, target stats
       - temporal_split(df, date_column, n_splits=5) -> list of (train_idx, test_idx) tuples: walk-forward CV respecting time order
       - validate_no_leakage(df, target_column, date_column=None) -> list[str]: check for potential leakage indicators (target in features, future-dated features without .shift())

    4. Create src/mlforge/tabular/baselines.py:
       - compute_baselines(X, y, scoring, task) -> dict[str, dict[str, float]]: per research code example -- DummyClassifier(most_frequent, stratified) for classification, DummyRegressor(mean, median) for regression, each with cross_val_score
       - passes_baseline_gate(metric_value, baselines, direction) -> bool: returns True only if metric beats ALL baseline scores (> for maximize, < for minimize)

    5. Create src/mlforge/templates/tabular_train.py.j2:
       - Renders valid Python script importing prepare module functions
       - Includes Optuna study creation with objective function skeleton
       - Imports sklearn, xgboost, lightgbm
       - Uses config variables: csv_path, target_column, metric, time_budget
       - Includes comment blocks explaining mutable zone rules

    6. Write tests FIRST (RED), then implement (GREEN):
       - tests/mlforge/test_tabular.py: Protocol conformance, scaffold output, template_context for classification/regression, validate_config valid/invalid, load_data CSV/Parquet, split_data shapes, build_preprocessor output, evaluate mock, temporal_split ordering, validate_no_leakage
       - tests/mlforge/test_baselines.py: compute_baselines classification/regression, passes_baseline_gate maximize/minimize/edge cases

    NOTE: For Parquet tests, create small test fixtures inline (pd.DataFrame saved to tmp parquet). For sklearn tests that need data, use sklearn.datasets.make_classification/make_regression with small n_samples (50-100).
  </action>
  <verify>
    <automated>python3 -m pytest tests/mlforge/test_tabular.py tests/mlforge/test_baselines.py -x -v</automated>
  </verify>
  <done>
    - TabularPlugin passes isinstance(TabularPlugin(), DomainPlugin) check
    - scaffold() creates prepare.py and train.py in target directory
    - template_context() returns task-specific domain_rules
    - validate_config() accepts valid metrics, rejects invalid ones
    - prepare.py handles CSV and Parquet loading, splitting, preprocessing, evaluation, and temporal utilities
    - compute_baselines() produces scores for naive and domain-specific strategies
    - passes_baseline_gate() correctly handles maximize and minimize directions
    - train.py template renders valid Python with Optuna boilerplate
    - All tests pass
  </done>
</task>

</tasks>

<verification>
python3 -m pytest tests/mlforge/ -x -q
</verification>

<success_criteria>
- TabularPlugin satisfies DomainPlugin Protocol (isinstance check passes)
- Plugin scaffolds both prepare.py (frozen) and train.py (mutable) to target directory
- Baseline computation and dual-baseline gate work for both classification and regression
- Leakage prevention utilities (temporal_split, validate_no_leakage) are functional
- All new + existing tests pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tabular-plugin-experiment-intelligence/02-01-SUMMARY.md`
</output>
