---
phase: 05-hooks-and-enhanced-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automl/templates/claude.md.tmpl
  - tests/test_templates.py
autonomous: true
requirements:
  - HOOK-05
  - HOOK-06

must_haves:
  truths:
    - "CLAUDE.md template includes a Graceful Shutdown section with instructions for max_turns interrupt"
    - "CLAUDE.md template still contains all existing protocol sections (draft phase, iterative loop, rules)"
    - "Graceful shutdown instructs agent to finish results.tsv row, check git status, and reset if mid-experiment"
  artifacts:
    - path: "src/automl/templates/claude.md.tmpl"
      provides: "Updated CLAUDE.md template with graceful shutdown section"
      contains: "Graceful Shutdown"
    - path: "tests/test_templates.py"
      provides: "Test asserting graceful shutdown section exists"
      contains: "graceful shutdown"
  key_links:
    - from: "src/automl/templates/claude.md.tmpl"
      to: "CLAUDE.md (rendered)"
      via: "render_claude_md() reads template file"
      pattern: "render_claude_md"
---

<objective>
Add a Graceful Shutdown section to the CLAUDE.md template so the autonomous agent handles max_turns interrupts cleanly, and update template tests.

Purpose: Phase 4 found stop_reason=tool_use (mid-action interrupt at max_turns). A graceful shutdown section tells the agent to finish writing results.tsv, check git status, and reset if mid-experiment — preventing partially-edited train.py states.
Output: Updated claude.md.tmpl with ~10 lines added, updated tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-e2e-baseline-test/FINDINGS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/automl/templates/__init__.py:
```python
def render_claude_md() -> str:
    """Render CLAUDE.md loop protocol (no substitution needed -- it is static)."""
    with open(os.path.join(_TEMPLATE_DIR, "claude.md.tmpl")) as f:
        return f.read()
```

From src/automl/templates/claude.md.tmpl (current sections):
```
# AutoML Experiment Loop Protocol
## Setup
## Files
## Phase 1: Multi-Draft Initialization
## Phase 2: Iterative Improvement Loop
## Rules
```
<!-- New section "## Graceful Shutdown" to be added before ## Rules -->

From tests/test_templates.py:
```python
class TestClaudeMdTemplate:
    # Tests: exists, NEVER STOP, LOOP, program.md, run.log, grep/metric,
    #        keep/revert, stagnation, crash, draft, train.py mutable, prepare.py frozen

class TestRenderFunctions:
    def test_render_claude_md(self):
        # Asserts: isinstance str, len > 100, "NEVER STOP" in result
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Graceful Shutdown section to CLAUDE.md template with tests</name>
  <files>src/automl/templates/claude.md.tmpl, tests/test_templates.py</files>
  <behavior>
    - Test: CLAUDE.md template contains "Graceful Shutdown" section header
    - Test: CLAUDE.md template mentions "results.tsv" in the shutdown section context
    - Test: CLAUDE.md template mentions "git reset --hard HEAD" in shutdown context
    - Test: CLAUDE.md template mentions "git status" in shutdown context
    - Test: render_claude_md() output contains "Graceful Shutdown"
    - Test: All existing template tests still pass (no removed content)
  </behavior>
  <action>
    **Tests first (RED):**
    Add to tests/test_templates.py in `TestClaudeMdTemplate` class:
    1. `test_claude_md_has_graceful_shutdown` — assert "Graceful Shutdown" in content
    2. `test_claude_md_shutdown_mentions_git_reset` — assert "git reset --hard HEAD" in content (shutdown recovery)
    3. `test_claude_md_shutdown_mentions_results_tsv` — assert "results.tsv" appears in shutdown-adjacent text (already present elsewhere, but verify shutdown section references finishing the row)

    Update `TestRenderFunctions.test_render_claude_md`:
    4. Add assertion: `assert "Graceful Shutdown" in result`

    **Implementation (GREEN):**
    Edit `src/automl/templates/claude.md.tmpl`:
    1. Add a new section `## Graceful Shutdown` between `## Phase 2: Iterative Improvement Loop` and `## Rules`.
    2. Content (per research recommendation):
    ```markdown
    ## Graceful Shutdown

    When you approach your turn limit or are interrupted mid-action:

    1. **Finish the current results.tsv row.** If you ran an experiment, log its result
       before stopping -- even if you are about to revert.
    2. **Run `git status`** to confirm `train.py` is in a clean state.
    3. **If train.py is partially edited** (uncommitted changes from a mid-experiment
       interrupt), run `git reset --hard HEAD` to restore the last known good state.
    4. **Do NOT leave train.py in a half-written state.** The next session must be able
       to resume from a clean commit.
    ```

    Keep all existing sections unchanged. The template should grow by ~10 lines.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_templates.py -x -q</automated>
  </verify>
  <done>
    - claude.md.tmpl contains "## Graceful Shutdown" section with git reset and results.tsv instructions
    - All existing template tests still pass (no content removed)
    - render_claude_md() includes graceful shutdown content
    - Template stays under ~100 lines (lean, per research guidance)
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_templates.py -x -q` — all template tests pass
- `uv run pytest tests/ -x -q` — full suite still green
</verification>

<success_criteria>
- CLAUDE.md template has Graceful Shutdown section addressing stop_reason=tool_use from Phase 4
- Section instructs agent to finish results.tsv row, check git status, reset if dirty
- All existing template content preserved (NEVER STOP, loop protocol, rules, etc.)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-hooks-and-enhanced-scaffolding/05-02-SUMMARY.md`
</output>
