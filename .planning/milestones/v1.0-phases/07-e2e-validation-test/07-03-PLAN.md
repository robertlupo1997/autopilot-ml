---
phase: 07-e2e-validation-test
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - .planning/phases/07-e2e-validation-test/FINDINGS.md
autonomous: false
gap_closure: true
requirements:
  - VAL-03
  - VAL-04
  - VAL-05
  - VAL-06
  - VAL-07

must_haves:
  truths:
    - "The autonomous loop ran unattended with experiments_run > 0 and permission_denials = 0"
    - "FINDINGS.md documents successful Phase 5-6 behavior (hooks, structured output, keep/revert, stagnation)"
    - "v1.0 certification status is updated based on successful run data"
  artifacts:
    - path: ".planning/phases/07-e2e-validation-test/FINDINGS.md"
      provides: "Populated validation findings with successful run data"
      contains: "experiments_run"
  key_links:
    - from: "scripts/run-validation-test.sh"
      to: ".planning/phases/07-e2e-validation-test/FINDINGS.md"
      via: "Human runs script, provides output, executor populates FINDINGS.md"
      pattern: "## v1.0 Certification"
---

<objective>
Re-run the validation test after the scaffold.py permissions fix and populate FINDINGS.md with successful run data proving the autonomous loop works end-to-end.

Purpose: Plan 07-02 fixed the permissions root cause. This plan re-runs the same validation script to confirm the fix works (experiments run, 0 denials), then documents the results in FINDINGS.md to close all remaining verification gaps and certify v1.0.

Output: Populated FINDINGS.md with successful validation data
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-validation-test/07-VERIFICATION.md
@.planning/phases/07-e2e-validation-test/FINDINGS.md
@.planning/phases/07-e2e-validation-test/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Re-run validation test with fixed permissions</name>
  <what-built>Plan 07-02 fixed scaffold.py to generate broader permissions (Bash(*), Write(results.tsv), Write(run.log)). The validation test script already exists and is correct -- only the generated settings.json was wrong.</what-built>
  <action>
USER ACTION REQUIRED: `claude -p` cannot be launched inside another Claude Code session.

IMPORTANT: Before running, the experiment directory from the previous failed run must be removed so the scaffold can recreate it with the fixed permissions.

The user must:
1. Open a NEW terminal (not inside Claude Code)
2. cd to the AutoML project root: `cd /home/tlupo/AutoML`
3. Remove the old experiment directory: `rm -rf experiment-noisy`
4. Run: `./scripts/run-validation-test.sh`
5. Wait for completion (estimated 5-15 minutes, capped at $4.00 / 50 turns)
6. The script will print automated assertions and diagnostics at the end
7. Return to this Claude Code session and provide:
   a. The automated assertion results (OK/FAIL/WARN lines from the script output)
   b. The parse_run_result.py output (stop_reason, num_turns, total_cost_usd)
   c. The contents of `experiment-noisy/results.tsv` (if it exists)
   d. The output of `git log --oneline` from inside experiment-noisy/
   e. The permission_denials count (should be 0 now)
   f. Whether json_output appeared in run.log
   g. Any stagnation observations (consecutive reverts in results.tsv)
   h. Any error messages or unexpected behavior
  </action>
  <how-to-verify>
    The script prints OK/FAIL/WARN for each automated check. Key expectations:
    - permission_denials: 0 (was 8 before fix)
    - experiments_run: > 0 (was 0 before fix)
    - stop_reason: end_turn or max_turns (not tool_use)
    - prepare.py unchanged
  </how-to-verify>
  <resume-signal>Paste the run output and observations, or type "skip" to proceed with placeholder FINDINGS.md.</resume-signal>
  <verify>
    <automated>test -f experiment-noisy/validation-run-output.json || echo "Output file will be created by user outside Claude Code"</automated>
  </verify>
  <done>User has run the validation test script after the permissions fix and provided the output data, OR user has typed "skip".</done>
</task>

<task type="auto">
  <name>Task 2: Re-populate FINDINGS.md with successful run data</name>
  <files>.planning/phases/07-e2e-validation-test/FINDINGS.md</files>
  <action>
Overwrite `.planning/phases/07-e2e-validation-test/FINDINGS.md` with the validated run data from Task 1. Use the same template structure as the existing FINDINGS.md but populate all sections with the successful run data.

Key sections to update from failure data to success data:

1. **Run Summary table**: Update experiments_run (should be > 0), stop_reason, num_turns, total_cost_usd, best_metric, frozen_file_compliance

2. **Hook Enforcement (Phase 5)**: Update permission_denials to 0 (was 8). Document whether the guard-frozen.sh hook fired (agent tried to write prepare.py) or was not triggered (CLAUDE.md instructions sufficient). Both are acceptable outcomes.

3. **Graceful Shutdown (Phase 5)**: Update stop_reason. If end_turn or max_turns from a run where experiments executed, mark as PASSED. Document git status at exit.

4. **Structured Output (Phase 6)**: Update json_output presence in run.log. If found, extract and document a sample json_output line.

5. **Draft Phase**: Document how many drafts were generated, which algorithm families, which was selected as best.

6. **Keep/Revert Cycle**: Document git commits on keep, git reset on revert from results.tsv status column.

7. **Stagnation Handling**: Count consecutive reverts in results.tsv. Document whether stagnation threshold (5) was reached and if strategy shift occurred.

8. **Comparison with Phase 4**: Update the comparison table with real Phase 7 success data. Show improvement over Phase 4 baseline.

9. **v1.0 Certification**: Based on findings, state whether v1.0 is certified or what remains.

10. **Raw Data**: Paste results.tsv, git log, and key JSON fields.

If user typed "skip" in Task 1, note FINDINGS.md as placeholder pending re-run.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && test -f .planning/phases/07-e2e-validation-test/FINDINGS.md && grep -q "## Hook Enforcement" .planning/phases/07-e2e-validation-test/FINDINGS.md && grep -q "## Graceful Shutdown" .planning/phases/07-e2e-validation-test/FINDINGS.md && grep -q "## v1.0 Certification" .planning/phases/07-e2e-validation-test/FINDINGS.md && grep -q "experiments_run" .planning/phases/07-e2e-validation-test/FINDINGS.md</automated>
  </verify>
  <done>FINDINGS.md is populated with successful validation run data. All Phase 5-6 sections document observed behavior (not failure data). v1.0 certification assessment reflects the actual run outcome. Comparison with Phase 4 shows improvement.</done>
</task>

</tasks>

<verification>
- FINDINGS.md contains updated data from the post-fix validation run
- experiments_run > 0 documented in FINDINGS.md
- permission_denials = 0 documented in FINDINGS.md
- All Phase 5-6 validation sections populated with real observations
- v1.0 certification status reflects the successful run
</verification>

<success_criteria>
- The validation test ran successfully after the permissions fix (experiments_run > 0, permission_denials = 0)
- FINDINGS.md documents all Phase 5-6 behaviors with real data from a successful run
- v1.0 certification assessment is written based on actual validated behavior
- Phase 4 vs Phase 7 comparison shows measurable improvement
</success_criteria>

<output>
After completion, create `.planning/phases/07-e2e-validation-test/07-03-SUMMARY.md`
</output>
