---
phase: 20-fix-multi-draft-dl-ft-task-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlforge/intelligence/drafts.py
  - src/mlforge/engine.py
  - tests/mlforge/test_drafts.py
  - tests/mlforge/test_engine.py
autonomous: true
requirements: [INTL-05, DL-04]

must_haves:
  truths:
    - "Multi-draft with --enable-drafts on a DL domain iterates DL model families (ResNet, ViT, EfficientNet), not tabular families"
    - "Multi-draft with --enable-drafts on a FT domain iterates FT adapter configs (QLoRA r8, r16, r32, LoRA full), not tabular families"
    - "Stagnation branching on a DL/FT domain picks untried families from the correct domain subset"
    - "Existing tabular multi-draft and stagnation behavior is unchanged"
  artifacts:
    - path: "src/mlforge/intelligence/drafts.py"
      provides: "Domain-keyed ALGORITHM_FAMILIES + get_families_for_domain helper"
      exports: ["ALGORITHM_FAMILIES", "get_families_for_domain", "DraftResult", "select_best_draft"]
    - path: "src/mlforge/engine.py"
      provides: "Domain-filtered draft phase and stagnation logic"
      contains: "get_families_for_domain"
  key_links:
    - from: "src/mlforge/engine.py"
      to: "src/mlforge/intelligence/drafts.py"
      via: "get_families_for_domain(self.config.domain)"
      pattern: "get_families_for_domain"
    - from: "src/mlforge/engine.py (_run_draft_phase)"
      to: "ALGORITHM_FAMILIES via helper"
      via: "domain-filtered iteration"
      pattern: "get_families_for_domain.*\\.items\\(\\)"
    - from: "src/mlforge/engine.py (_process_result stagnation)"
      to: "ALGORITHM_FAMILIES via helper"
      via: "domain-filtered untried check"
      pattern: "get_families_for_domain.*tried_families"
---

<objective>
Add DL and FT task type entries to ALGORITHM_FAMILIES and wire domain-aware family selection into engine draft phase and stagnation logic.

Purpose: Multi-draft and stagnation currently only work for tabular domains. DL/FT domains fall back to raw family name strings in prompts, producing meaningless draft prompts. This fix enables proper multi-draft exploration across all three domains.

Output: Updated drafts.py with domain-keyed families and get_families_for_domain helper; updated engine.py using the helper in both _run_draft_phase and stagnation; updated tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-fix-multi-draft-dl-ft-task-keys/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/mlforge/intelligence/drafts.py:
```python
ALGORITHM_FAMILIES: dict[str, dict[str, str]] = {
    "linear": {
        "description": "Linear models (Ridge/LogisticRegression)",
        "classification": "LogisticRegression",
        "regression": "Ridge",
    },
    # ... 5 tabular families total
}

@dataclass
class DraftResult:
    name: str
    metric_value: float | None
    status: str
    commit_hash: str
    description: str

def select_best_draft(results: list[DraftResult], direction: str = "maximize") -> DraftResult | None: ...
```

From src/mlforge/engine.py (lines 22-23, import):
```python
from mlforge.intelligence.drafts import ALGORITHM_FAMILIES, DraftResult, select_best_draft
```

From src/mlforge/engine.py (line 267, stagnation):
```python
untried = [f for f in ALGORITHM_FAMILIES if f not in self.state.tried_families]
```

From src/mlforge/engine.py (line 322, draft phase):
```python
for family_name, family_info in ALGORITHM_FAMILIES.items():
```

From src/mlforge/config.py (Config.domain):
```python
domain: str = "tabular"  # "tabular", "deeplearning", "finetuning"
```

From src/mlforge/scaffold.py (task type mapping reference):
```python
_TASK_TYPE_MAP = {
    "deeplearning": {"classification": "image_classification", "regression": "image_classification"},
    "finetuning": {"classification": "sft", "regression": "sft"},
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure ALGORITHM_FAMILIES as domain-keyed dict + add get_families_for_domain helper</name>
  <files>src/mlforge/intelligence/drafts.py, tests/mlforge/test_drafts.py</files>
  <behavior>
    - Test: get_families_for_domain("tabular") returns dict with keys linear, random_forest, xgboost, lightgbm, svm
    - Test: get_families_for_domain("deeplearning") returns dict with keys resnet, vit, efficientnet (each with image_classification, text_classification, custom task keys)
    - Test: get_families_for_domain("finetuning") returns dict with keys qlora_r8, qlora_r16, qlora_r32, lora_full (each with sft task key)
    - Test: get_families_for_domain("unknown_domain") falls back to tabular families
    - Test: Each family entry has a "description" key
    - Test: DL family entries have correct model class values (e.g., resnet50 for image_classification)
    - Test: FT family entries have correct adapter config values (e.g., "QLoRA r=8 alpha=8" for sft)
  </behavior>
  <action>
Restructure ALGORITHM_FAMILIES in src/mlforge/intelligence/drafts.py from a flat dict to a domain-keyed nested dict with three top-level keys: "tabular", "deeplearning", "finetuning".

Tabular families stay identical (linear, random_forest, xgboost, lightgbm, svm) with classification/regression task keys.

Add deeplearning families (per research):
- "resnet": image_classification -> "resnet50", text_classification -> "distilbert-base-uncased", custom -> "resnet50"
- "vit": image_classification -> "vit_base_patch16_224", text_classification -> "bert-base-uncased", custom -> "vit_base_patch16_224"
- "efficientnet": image_classification -> "efficientnet_b0", text_classification -> "roberta-base", custom -> "efficientnet_b0"

Add finetuning families (per research):
- "qlora_r8": sft -> "QLoRA r=8 alpha=8"
- "qlora_r16": sft -> "QLoRA r=16 alpha=16"
- "qlora_r32": sft -> "QLoRA r=32 alpha=32"
- "lora_full": sft -> "LoRA r=16 alpha=16 (no quantization)"

Add get_families_for_domain(domain: str) -> dict[str, dict[str, str]] helper that returns ALGORITHM_FAMILIES.get(domain, ALGORITHM_FAMILIES.get("tabular", {})).

Update type annotation: ALGORITHM_FAMILIES: dict[str, dict[str, dict[str, str]]].

Update existing tests in TestAlgorithmFamilies to use get_families_for_domain("tabular") instead of direct ALGORITHM_FAMILIES access (since the top-level keys changed). Add new test class TestGetFamiliesForDomain covering all domain lookups and fallback behavior. Add tests for DL and FT family entries having correct task keys and descriptions.
  </action>
  <verify>
    <automated>python -m pytest tests/mlforge/test_drafts.py -x -q</automated>
  </verify>
  <done>ALGORITHM_FAMILIES has tabular/deeplearning/finetuning top-level keys. get_families_for_domain returns correct subset per domain. All draft tests pass including new DL/FT coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire get_families_for_domain into engine draft phase + stagnation</name>
  <files>src/mlforge/engine.py, tests/mlforge/test_engine.py</files>
  <behavior>
    - Test: _run_draft_phase iterates domain-appropriate families (mock config.domain="deeplearning" -> iterates DL families, not tabular)
    - Test: Stagnation untried check uses domain-filtered families (config.domain="deeplearning" -> only DL families considered)
    - Test: Existing tabular draft/stagnation tests still pass (config.domain="tabular" by default)
    - Test: _build_draft_prompt renders correct model class for DL task (e.g., "resnet50" not "resnet")
  </behavior>
  <action>
In src/mlforge/engine.py:

1. Update import (line 23): Change `from mlforge.intelligence.drafts import ALGORITHM_FAMILIES, DraftResult, select_best_draft` to `from mlforge.intelligence.drafts import ALGORITHM_FAMILIES, DraftResult, get_families_for_domain, select_best_draft`.

2. In _run_draft_phase (line 322): Replace `for family_name, family_info in ALGORITHM_FAMILIES.items():` with `families = get_families_for_domain(self.config.domain)` then `for family_name, family_info in families.items():`.

3. In _process_result stagnation block (line 267): Replace `untried = [f for f in ALGORITHM_FAMILIES if f not in self.state.tried_families]` with `families = get_families_for_domain(self.config.domain)` then `untried = [f for f in families if f not in self.state.tried_families]`.

Update tests in test_engine.py:

- test_stagnation_picks_untried_family: Update the ALGORITHM_FAMILIES mock to use the new nested structure or mock get_families_for_domain instead. Ensure it tests domain-filtered behavior.
- test_draft_runs_each_family: Verify it iterates only families for config.domain (default "tabular").
- test_tried_families_populated: Same -- verify only tabular families added to tried_families when domain is tabular.
- Add new test: test_draft_phase_uses_dl_families -- set config.domain="deeplearning", verify _run_draft_phase iterates DL families (resnet, vit, efficientnet).
- Add new test: test_stagnation_uses_domain_families -- set config.domain="deeplearning", verify untried list only contains DL families.
- Add new test: test_draft_prompt_renders_dl_model_class -- verify _build_draft_prompt with DL family info and task="image_classification" renders "resnet50" not "resnet".

IMPORTANT: The existing mock `patch("mlforge.engine.ALGORITHM_FAMILIES", ...)` in test_stagnation_picks_untried_family must be changed to `patch("mlforge.engine.get_families_for_domain", return_value=...)` since engine now calls the helper, not the dict directly.
  </action>
  <verify>
    <automated>python -m pytest tests/mlforge/test_drafts.py tests/mlforge/test_engine.py -x -q</automated>
  </verify>
  <done>Engine draft phase and stagnation logic use get_families_for_domain for domain-aware family selection. DL/FT domains get correct families. All existing tabular tests pass. New DL/FT engine tests pass.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/mlforge/test_drafts.py tests/mlforge/test_engine.py -x -q` -- all draft and engine tests pass
2. `python -m pytest tests/mlforge/ -x -q` -- full test suite green (no regressions)
3. `python -c "from mlforge.intelligence.drafts import get_families_for_domain; families = get_families_for_domain('deeplearning'); print(list(families.keys()))"` -- prints ['resnet', 'vit', 'efficientnet']
4. `python -c "from mlforge.intelligence.drafts import get_families_for_domain; families = get_families_for_domain('finetuning'); print(list(families.keys()))"` -- prints ['qlora_r8', 'qlora_r16', 'qlora_r32', 'lora_full']
</verification>

<success_criteria>
- ALGORITHM_FAMILIES has domain-keyed structure with tabular, deeplearning, and finetuning entries
- get_families_for_domain helper returns correct subset per domain with tabular fallback
- Engine _run_draft_phase iterates domain-appropriate families
- Engine stagnation logic filters untried families by domain
- DL families have image_classification, text_classification, custom task keys
- FT families have sft task key
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/20-fix-multi-draft-dl-ft-task-keys/20-01-SUMMARY.md`
</output>
