---
phase: 24
plan: 1
title: Remove dead code (to_json, from_json, temporal_split, unused import)
requirements: []
estimated_tasks: 3
---

# Plan: Tech Debt Cleanup -- Remove Dead Code

## Goal
Remove dead code identified by v1.0 milestone audit to reduce maintenance burden.

## Files to Modify
- `src/mlforge/state.py` -- remove `to_json()` and `from_json()` methods from SessionState
- `src/mlforge/tabular/prepare.py` -- remove `temporal_split()` function
- `src/mlforge/engine.py` -- remove unused `ALGORITHM_FAMILIES` from import on line 23

## Implementation

### Task 1: Remove SessionState.to_json() and from_json()
These methods are dead code -- checkpoint.py uses `dataclasses.asdict()` instead.
Grep for all usages first to confirm no callers exist.

### Task 2: Remove temporal_split() from tabular/prepare.py
This function is never called -- only referenced in a template comment.
Grep to confirm no callers exist (template comment doesn't count as a caller).

### Task 3: Remove unused ALGORITHM_FAMILIES import from engine.py
Line 23 imports both `ALGORITHM_FAMILIES` and `get_families_for_domain`. Only the latter is used.
Remove `ALGORITHM_FAMILIES` from the import statement, keeping `get_families_for_domain`.

## Verification
- All existing tests still pass after each removal
- No new test failures introduced
- Grep confirms removed items have no remaining references in src/
