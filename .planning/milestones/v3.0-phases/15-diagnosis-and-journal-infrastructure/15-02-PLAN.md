---
phase: 15-diagnosis-and-journal-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automl/templates/experiments.md.tmpl
  - src/automl/templates/__init__.py
  - src/automl/scaffold.py
  - tests/test_scaffold.py
autonomous: true
requirements:
  - KNOW-01
  - KNOW-03

must_haves:
  truths:
    - "Every newly scaffolded experiment directory contains an experiments.md file"
    - "experiments.md has four sections: What Works, What Doesn't, Hypotheses Queue, Error Patterns"
    - "experiments.md is pre-populated with dataset summary and baseline scores from scaffold"
  artifacts:
    - path: "src/automl/templates/experiments.md.tmpl"
      provides: "Journal template with 4 required sections"
      contains: "What Works"
    - path: "src/automl/templates/__init__.py"
      provides: "render_experiments_md function"
      exports: ["render_experiments_md"]
    - path: "src/automl/scaffold.py"
      provides: "experiments.md generation in scaffold_experiment"
      contains: "experiments.md"
    - path: "tests/test_scaffold.py"
      provides: "Tests for experiments.md in scaffold"
      contains: "experiments.md"
  key_links:
    - from: "src/automl/scaffold.py"
      to: "src/automl/templates/__init__.py"
      via: "import render_experiments_md"
      pattern: "render_experiments_md"
    - from: "src/automl/scaffold.py"
      to: "src/automl/templates/experiments.md.tmpl"
      via: "template rendering with dataset context"
      pattern: "experiments.md"
---

<objective>
Create the experiments.md journal template and integrate it into scaffold so every new experiment starts with a structured knowledge capture file pre-populated with dataset context.

Purpose: KNOW-01 + KNOW-03 — the agent needs a persistent journal to accumulate experiment knowledge across iterations. Scaffold pre-populates it so the agent starts informed rather than blank.
Output: New template file, updated scaffold, updated tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/automl/scaffold.py
@src/automl/templates/__init__.py
@tests/test_scaffold.py
</context>

<interfaces>
<!-- Existing scaffold.py interface -->

From src/automl/scaffold.py:
```python
def scaffold_experiment(
    data_path, target_column, metric, goal, output_dir=None, time_budget=60, date_col=None
) -> Path
```

From src/automl/templates/__init__.py:
```python
def render_program_md(...) -> str
def render_claude_md() -> str
def render_claude_md_forecast() -> str
```

<!-- New function to add -->

```python
def render_experiments_md(
    dataset_name: str,
    data_summary: str,
    baselines: str,
) -> str:
    """Render experiments.md journal template with pre-populated context."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create experiments.md template and render function</name>
  <files>src/automl/templates/experiments.md.tmpl, src/automl/templates/__init__.py</files>
  <behavior>
    - render_experiments_md(dataset_name, data_summary, baselines) returns a string
    - Output contains "# Experiment Journal: {dataset_name}" header
    - Output contains all 4 required section headers: "## What Works", "## What Doesn't", "## Hypotheses Queue", "## Error Patterns"
    - Output contains "## Dataset Context" section with data_summary and baselines pre-populated
    - Each section has a brief instruction comment explaining what goes there
    - Baselines section shows the numeric baseline values so agent knows what to beat
  </behavior>
  <action>
    Create `src/automl/templates/experiments.md.tmpl` with this structure:
    - H1: "Experiment Journal: {dataset_name}"
    - H2: "Dataset Context" — contains {data_summary} and {baselines} placeholders
    - H2: "What Works" — instruction: record techniques/hyperparameters that improved score
    - H2: "What Doesn't" — instruction: record approaches that hurt or had no effect
    - H2: "Hypotheses Queue" — instruction: list next ideas to try with rationale
    - H2: "Error Patterns" — instruction: record diagnostic output patterns (ties to DIAG-01)

    Add `render_experiments_md(dataset_name, data_summary, baselines)` to `src/automl/templates/__init__.py` following the exact pattern of `render_program_md` — reads the .tmpl file, calls `.format()` with the arguments.

    Write tests first: import render_experiments_md, call it with test data, assert all 4 section headers present, assert dataset_name appears, assert data_summary and baselines appear in output.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/test_scaffold.py -x -v -k "experiments_md_template or render_experiments"</automated>
  </verify>
  <done>render_experiments_md() returns properly formatted journal with all 4 sections and pre-populated dataset context</done>
</task>

<task type="auto">
  <name>Task 2: Integrate experiments.md into scaffold_experiment</name>
  <files>src/automl/scaffold.py, tests/test_scaffold.py</files>
  <action>
    Modify `scaffold_experiment()` in `scaffold.py` to generate `experiments.md` in both the forecasting path (date_col is not None) and the standard v1.0 path (date_col is None):

    1. Add import: `from automl.templates import render_experiments_md` (add to existing import line 29)
    2. In the forecasting path (after line 145 where CLAUDE.md is written), add:
       ```python
       experiments_md = render_experiments_md(
           dataset_name=csv_path.stem,
           data_summary=summary_str,
           baselines=baselines_str,
       )
       (out / "experiments.md").write_text(experiments_md)
       ```
    3. In the standard v1.0 path (after line 192 where CLAUDE.md is written), add the same pattern using the existing `summary_str` and `baselines_str` variables.
    4. Update `_gitignore_content()` — do NOT add experiments.md to gitignore (it should be committed).
    5. Update test `TestScaffoldCreatesAllFiles.test_scaffold_creates_all_files` to expect `experiments.md` in the file list and update the count from 9 to 10 (8 files + experiments.md + .claude/ dir).
    6. Add new test class `TestScaffoldExperimentsMd` with tests:
       - `test_scaffold_creates_experiments_md` — file exists in scaffolded dir
       - `test_experiments_md_has_four_sections` — all 4 section headers present
       - `test_experiments_md_has_dataset_context` — data summary and baselines present
       - `test_forecast_scaffold_creates_experiments_md` — same checks for forecasting path (use sample_forecast_csv fixture if exists, or create one)
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/test_scaffold.py -x -v</automated>
  </verify>
  <done>Both standard and forecasting scaffold paths produce experiments.md with 4 sections and pre-populated dataset context. All scaffold tests pass including existing ones (no regressions).</done>
</task>

</tasks>

<verification>
cd /home/tlupo/AutoML && python -m pytest tests/test_forecast.py tests/test_scaffold.py -x -v
</verification>

<success_criteria>
- `experiments.md.tmpl` exists with 4 required sections
- `render_experiments_md()` importable from automl.templates
- scaffold_experiment() creates experiments.md in both paths
- experiments.md contains dataset summary and baseline scores
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/15-diagnosis-and-journal-infrastructure/15-02-SUMMARY.md`
</output>
