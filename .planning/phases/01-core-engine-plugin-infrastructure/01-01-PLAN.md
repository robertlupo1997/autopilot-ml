---
phase: 01-core-engine-plugin-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mlforge/__init__.py
  - src/mlforge/state.py
  - src/mlforge/config.py
  - src/mlforge/checkpoint.py
  - tests/conftest.py
  - tests/test_state.py
  - tests/test_config.py
  - tests/test_checkpoint.py
autonomous: true
requirements: [CORE-04, CORE-06, CORE-05]

must_haves:
  truths:
    - "SessionState can be created with defaults, serialized to JSON, and deserialized back with all fields intact"
    - "SessionState uses atomic write-then-rename to prevent corruption on crash"
    - "Config loads from mlforge.config.toml with all fields populated, falling back to defaults when file is missing"
    - "Config validates domain, metric direction, and budget values"
    - "A checkpoint can be saved and loaded, restoring SessionState to the exact pre-crash state"
    - "Checkpoint handles schema evolution by ignoring unknown fields from newer versions"
  artifacts:
    - path: "src/mlforge/__init__.py"
      provides: "Package root with version"
    - path: "src/mlforge/state.py"
      provides: "SessionState dataclass with JSON persistence"
      exports: ["SessionState"]
    - path: "src/mlforge/config.py"
      provides: "Config dataclass with TOML loading"
      exports: ["Config", "CONFIG_FILENAME"]
    - path: "src/mlforge/checkpoint.py"
      provides: "Checkpoint save/load with schema versioning"
      exports: ["save_checkpoint", "load_checkpoint"]
    - path: "pyproject.toml"
      provides: "Package metadata for mlforge with gitpython and jinja2 deps"
  key_links:
    - from: "src/mlforge/checkpoint.py"
      to: "src/mlforge/state.py"
      via: "imports SessionState for serialization"
      pattern: "from mlforge\\.state import SessionState"
    - from: "src/mlforge/config.py"
      to: "mlforge.config.toml"
      via: "tomllib.load reads config file"
      pattern: "tomllib\\.load"
---

<objective>
Create the mlforge package skeleton with the three foundational modules: SessionState (JSON-persisted experiment state), Config (TOML-based configuration), and Checkpoint (crash-resilient save/load). These are the data backbone that every other module depends on.

Purpose: All subsequent modules need state tracking, configuration, and checkpoint capabilities. Building these first with thorough tests establishes the foundation.
Output: Working mlforge package with state.py, config.py, checkpoint.py, and full test coverage.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine-plugin-infrastructure/01-RESEARCH.md
@.planning/phases/01-core-engine-plugin-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package skeleton + SessionState + Config</name>
  <files>pyproject.toml, src/mlforge/__init__.py, src/mlforge/state.py, src/mlforge/config.py, tests/conftest.py, tests/test_state.py, tests/test_config.py</files>
  <behavior>
    - SessionState() creates instance with defaults (experiment_count=0, best_metric=None, budget_remaining=0.0, etc.)
    - state.to_json(path) writes JSON file; SessionState.from_json(path) restores identical state
    - to_json uses atomic write-then-rename (.json.tmp -> .json)
    - from_json ignores unknown fields (forward compatibility)
    - from_json raises FileNotFoundError for missing file
    - Config.load() with no file returns defaults (domain="tabular", metric="accuracy", direction="maximize", budget_minutes=60, budget_experiments=50)
    - Config.load(path) reads a valid mlforge.config.toml and populates all fields from nested TOML tables
    - Config.load(path) with partial TOML fills missing fields from defaults
    - Config validates direction is "maximize" or "minimize"
  </behavior>
  <action>
    1. Update pyproject.toml: rename package to "mlforge", update description to "Autonomous ML research framework", add dependencies (gitpython>=3.1, jinja2>=3.1), update entry point to mlforge.cli:main, update hatch build packages to src/mlforge. Keep existing dev deps (pytest). Add Python >=3.11 requirement.

    2. Create src/mlforge/__init__.py with __version__ = "0.1.0" and package docstring.

    3. Create tests/conftest.py with shared fixtures:
       - tmp_dir fixture (tmp_path based)
       - sample_config_toml fixture returning a valid TOML string
       - sample_state fixture returning a populated SessionState

    4. Create src/mlforge/state.py following the research Pattern 1 exactly:
       - SessionState dataclass with fields: experiment_count (int, 0), best_metric (float|None, None), best_commit (str|None, None), budget_remaining (float, 0.0), consecutive_reverts (int, 0), total_keeps (int, 0), total_reverts (int, 0), run_id (str, "")
       - to_json(path: Path) using write-then-rename (write to .json.tmp, then Path.rename)
       - from_json(path: Path) classmethod using fields() filtering for forward compat

    5. Create src/mlforge/config.py following the research TOML Config Loading example:
       - CONFIG_FILENAME = "mlforge.config.toml"
       - Config dataclass with fields: domain (str, "tabular"), metric (str, "accuracy"), direction (str, "maximize"), budget_minutes (int, 60), budget_experiments (int, 50), frozen_files (list[str], ["prepare.py"]), mutable_files (list[str], ["train.py"]), plugin_settings (dict, {})
       - Config.load(path: Path | None) classmethod: returns defaults if no file, reads TOML with tomllib if file exists, flattens nested tables (metric.name, metric.direction, budget.minutes, budget.experiments, files.frozen, files.mutable, plugin.*)
       - Validate direction in ("maximize", "minimize"), raise ValueError otherwise

    6. Write tests first (TDD): test_state.py with 5+ tests covering round-trip, atomic write, forward compat, missing file. test_config.py with 5+ tests covering defaults, full TOML, partial TOML, validation error, missing file.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/test_state.py tests/test_config.py -x -v</automated>
  </verify>
  <done>pyproject.toml updated for mlforge. SessionState round-trips through JSON with atomic writes. Config loads from TOML with defaults and validation. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checkpoint save/load with schema versioning</name>
  <files>src/mlforge/checkpoint.py, tests/test_checkpoint.py</files>
  <behavior>
    - save_checkpoint(state, path) writes checkpoint.json containing {"schema_version": 1, "state": {...}, "timestamp": "..."}
    - save_checkpoint uses atomic write-then-rename
    - load_checkpoint(path) returns SessionState restored from checkpoint.json
    - load_checkpoint returns None if no checkpoint file exists (clean start)
    - load_checkpoint ignores unknown fields in state dict (forward compat via SessionState.from_json logic)
    - load_checkpoint with corrupt JSON raises ValueError with descriptive message
    - Checkpoint file includes schema_version for future migration support
  </behavior>
  <action>
    1. Write tests/test_checkpoint.py first (TDD):
       - test_save_and_load_roundtrip: save state, load it back, verify all fields match
       - test_load_missing_checkpoint_returns_none: load from nonexistent dir returns None
       - test_checkpoint_contains_schema_version: save, read raw JSON, verify schema_version=1
       - test_checkpoint_contains_timestamp: save, read raw JSON, verify ISO timestamp present
       - test_load_ignores_unknown_state_fields: manually write checkpoint with extra fields, load succeeds
       - test_load_corrupt_json_raises: write garbage to checkpoint.json, load raises ValueError
       - test_save_is_atomic: verify .json.tmp does not exist after save (renamed away)

    2. Create src/mlforge/checkpoint.py:
       - CHECKPOINT_FILE = "checkpoint.json"
       - SCHEMA_VERSION = 1
       - save_checkpoint(state: SessionState, checkpoint_dir: Path) -> None: creates dir if needed, writes {"schema_version": SCHEMA_VERSION, "state": asdict(state), "timestamp": datetime.now(timezone.utc).isoformat()} using atomic write-then-rename
       - load_checkpoint(checkpoint_dir: Path) -> SessionState | None: returns None if file missing, reads JSON, extracts state dict, constructs SessionState using field filtering (same pattern as from_json), raises ValueError on corrupt JSON
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/test_checkpoint.py -x -v</automated>
  </verify>
  <done>Checkpoint save/load works with schema versioning. Atomic writes prevent corruption. Forward-compatible deserialization handles unknown fields. All tests pass.</done>
</task>

</tasks>

<verification>
cd /home/tlupo/AutoML && python -m pytest tests/test_state.py tests/test_config.py tests/test_checkpoint.py -v
</verification>

<success_criteria>
- `python -c "import mlforge; print(mlforge.__version__)"` prints "0.1.0"
- SessionState round-trips through JSON with no data loss
- Config loads from TOML and falls back to sensible defaults
- Checkpoint save/load survives simulated crash (atomic writes)
- All tests pass (target: 15+ tests across 3 test files)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine-plugin-infrastructure/01-01-SUMMARY.md`
</output>
