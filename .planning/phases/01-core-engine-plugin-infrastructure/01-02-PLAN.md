---
phase: 01-core-engine-plugin-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlforge/git_ops.py
  - src/mlforge/journal.py
  - tests/test_git_ops.py
  - tests/test_journal.py
autonomous: true
requirements: [CORE-10, CORE-08]

must_haves:
  truths:
    - "GitManager creates a named branch for a run and checks it out"
    - "GitManager commits specified files with a message and returns the short hash"
    - "GitManager reverts working tree to the last commit (hard reset)"
    - "GitManager tags the current commit as best model"
    - "GitManager detects and avoids detached HEAD state after operations"
    - "Journal appends structured entries (hypothesis, result, metric, status) to JSONL file"
    - "Journal loads all entries from JSONL, returning empty list for missing file"
    - "Journal entries include timestamp and experiment_id"
  artifacts:
    - path: "src/mlforge/git_ops.py"
      provides: "GitManager class wrapping GitPython"
      exports: ["GitManager"]
    - path: "src/mlforge/journal.py"
      provides: "JSONL experiment journal with append and load"
      exports: ["append_journal_entry", "load_journal", "JournalEntry"]
  key_links:
    - from: "src/mlforge/git_ops.py"
      to: "git.Repo"
      via: "GitPython library"
      pattern: "from git import Repo"
    - from: "src/mlforge/journal.py"
      to: "experiments.jsonl"
      via: "JSONL append/read"
      pattern: "json\\.dumps.*json\\.loads"
---

<objective>
Build the git operations manager (GitPython-based branch/commit/revert/tag) and the experiment journal (JSONL append-only log). These are independent modules that track experiment state through git and structured knowledge through journal entries.

Purpose: Git ops provide the keep/revert mechanism central to the experiment loop. The journal accumulates structured knowledge that survives context resets and informs the agent's next hypothesis.
Output: Working git_ops.py and journal.py with integration and unit tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine-plugin-infrastructure/01-RESEARCH.md
@.planning/phases/01-core-engine-plugin-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GitManager with GitPython</name>
  <files>src/mlforge/git_ops.py, tests/test_git_ops.py</files>
  <behavior>
    - GitManager(repo_path) initializes with a GitPython Repo object
    - create_run_branch(run_id) creates branch "mlforge/run-{run_id}" and checks it out, returns branch name
    - create_run_branch with duplicate run_id raises ValueError
    - commit_experiment(message, files) stages specified files, commits, returns short hash (8 chars)
    - commit_experiment with no changes raises ValueError (nothing to commit)
    - revert_to_last_commit() does hard reset of index and working tree
    - After revert, repo.head.is_detached is False (stays on branch)
    - tag_best(tag_name, message) creates annotated tag on current HEAD
    - tag_best with existing tag name raises ValueError
    - close() calls repo.close() to prevent file handle leaks
    - GitManager works as context manager (__enter__/__exit__ with close)
  </behavior>
  <action>
    1. Add a git_repo fixture to tests/conftest.py (or tests/test_git_ops.py locally): creates a tmp directory, initializes a bare git repo with git init, creates an initial commit so HEAD exists. Use GitPython for setup: Repo.init(path), create a file, index.add, index.commit.

    2. Write tests/test_git_ops.py first (TDD):
       - test_create_run_branch: creates branch, verifies active branch name matches
       - test_create_run_branch_duplicate_raises: creating same branch twice raises ValueError
       - test_commit_experiment: create file, commit, verify short hash returned and file is committed
       - test_commit_no_changes_raises: commit with no staged changes raises ValueError
       - test_revert_to_last_commit: modify file, revert, verify file restored to committed state
       - test_revert_stays_on_branch: after revert, repo.head.is_detached is False
       - test_tag_best: tag current commit, verify tag exists and points to HEAD
       - test_tag_duplicate_raises: creating same tag twice raises ValueError
       - test_context_manager: use `with GitManager(path) as gm:` verify it works and closes

    3. Create src/mlforge/git_ops.py following research Pattern 2:
       - Import from git: Repo, Actor, InvalidGitRepositoryError, GitCommandError
       - GitManager class with __init__(repo_path: str | Path = ".") storing Repo object
       - create_run_branch(run_id: str) -> str: check if branch exists first (raise ValueError), create_head + checkout
       - commit_experiment(message: str, files: list[str]) -> str: check diff (raise ValueError if clean), index.add(files), index.commit(message), return hexsha[:8]
       - revert_to_last_commit() -> None: self.repo.head.reset(index=True, working_tree=True)
       - tag_best(tag_name: str, message: str = "") -> None: check tag exists (raise ValueError), create_tag
       - close() -> None: self.repo.close()
       - __enter__/__exit__ for context manager pattern

    IMPORTANT: Per research Pitfall 3, always verify repo.head.is_detached is False after reset operations. Per Pitfall 1, implement close() and context manager to prevent file handle leaks.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/test_git_ops.py -x -v</automated>
  </verify>
  <done>GitManager creates branches, commits, reverts, and tags using GitPython. Context manager prevents leaks. All edge cases tested. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Experiment journal with JSONL persistence</name>
  <files>src/mlforge/journal.py, tests/test_journal.py</files>
  <behavior>
    - append_journal_entry writes a single JSON line to experiments.jsonl
    - Each entry contains: experiment_id, timestamp (UTC ISO), hypothesis, result, metric_value, metric_delta, commit_hash, status
    - load_journal returns list of dicts from JSONL file
    - load_journal returns empty list for missing file
    - load_journal skips blank lines gracefully
    - Multiple appends create multiple lines (append-only, no rewrite)
    - JournalEntry dataclass provides typed construction
    - render_journal_markdown returns human-readable markdown table from entries
  </behavior>
  <action>
    1. Write tests/test_journal.py first (TDD):
       - test_append_and_load_single_entry: append one entry, load, verify fields match
       - test_append_multiple_entries: append 3 entries, load, verify count and order
       - test_load_missing_file_returns_empty: load from nonexistent path returns []
       - test_load_skips_blank_lines: write JSONL with blank lines interspersed, load succeeds
       - test_entry_has_timestamp: append entry, load, verify timestamp is valid ISO format
       - test_entry_has_all_fields: verify experiment_id, hypothesis, result, metric_value, metric_delta, commit_hash, status all present
       - test_journal_entry_dataclass: construct JournalEntry, verify fields
       - test_render_markdown: append 2 entries, render markdown, verify it contains a table with experiment IDs

    2. Create src/mlforge/journal.py following research Experiment Journal example:
       - JOURNAL_FILE = "experiments.jsonl"
       - JournalEntry dataclass with fields: experiment_id (int), hypothesis (str), result (str), metric_value (float|None), metric_delta (float|None), commit_hash (str|None), status (str: "keep"|"revert"|"crash")
       - append_journal_entry(path: Path, entry: JournalEntry) -> None: adds timestamp, writes asdict as JSON line. Note: take JournalEntry instead of individual args for cleaner API.
       - load_journal(path: Path) -> list[dict]: reads JSONL, skips blank lines, returns list of dicts
       - render_journal_markdown(entries: list[dict]) -> str: renders entries as markdown table with columns: #, Status, Metric, Delta, Hypothesis (truncated to 60 chars), Commit
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/test_journal.py -x -v</automated>
  </verify>
  <done>Journal appends structured entries to JSONL, loads them back, handles missing files gracefully, and renders human-readable markdown. All tests pass.</done>
</task>

</tasks>

<verification>
cd /home/tlupo/AutoML && python -m pytest tests/test_git_ops.py tests/test_journal.py -v
</verification>

<success_criteria>
- GitManager branch/commit/revert/tag operations work on real git repos in tmp dirs
- GitManager handles error cases (duplicate branch, duplicate tag, no changes) with clear ValueError messages
- Journal appends entries atomically and loads them back with all fields
- Journal handles missing file and blank lines gracefully
- All tests pass (target: 17+ tests across 2 test files)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine-plugin-infrastructure/01-02-SUMMARY.md`
</output>
