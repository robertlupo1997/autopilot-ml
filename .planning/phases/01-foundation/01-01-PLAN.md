---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/automl/__init__.py
  - src/automl/prepare.py
  - tests/conftest.py
  - tests/test_prepare.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - PIPE-07

must_haves:
  truths:
    - "load_data() reads any CSV and returns features, target, and inferred task type"
    - "split_data() produces a holdout set the agent never touches and a working set for CV"
    - "evaluate() returns cross-validated metric scores using any sklearn scoring string"
    - "get_baselines() computes dummy classifier/regressor scores for comparison"
    - "build_preprocessor() handles missing values and categorical encoding without leakage"
    - "get_data_summary() returns shape, dtypes, missing counts, and target distribution"
    - "validate_metric() maps user-facing metric names to sklearn scoring strings with task validation"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project config with all dependencies"
      contains: "scikit-learn"
    - path: "src/automl/prepare.py"
      provides: "Frozen data pipeline with all PIPE-* functions"
      exports: ["load_data", "split_data", "evaluate", "get_baselines", "build_preprocessor", "get_data_summary", "validate_metric", "METRIC_MAP"]
      min_lines: 150
    - path: "tests/conftest.py"
      provides: "Shared test fixtures (sample CSVs, temp directories)"
      min_lines: 30
    - path: "tests/test_prepare.py"
      provides: "Tests for all PIPE-01 through PIPE-07 behaviors"
      min_lines: 100
  key_links:
    - from: "tests/test_prepare.py"
      to: "src/automl/prepare.py"
      via: "import"
      pattern: "from automl\\.prepare import"
    - from: "src/automl/prepare.py"
      to: "sklearn"
      via: "cross_val_score, DummyClassifier, DummyRegressor, OrdinalEncoder, SimpleImputer"
      pattern: "from sklearn"
---

<objective>
Build the frozen data pipeline (prepare.py) and project scaffolding with full test coverage.

Purpose: prepare.py is the foundational module that train.py (Plan 03) imports from. It contains all frozen data operations: loading, splitting, preprocessing, evaluation, baselines, and data summary. Nothing else can run without this.

Output: A working `src/automl/prepare.py` with all PIPE-* functions, plus a pyproject.toml with dependencies installed, and passing tests for every function.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding, dependencies, and test fixtures</name>
  <files>pyproject.toml, src/automl/__init__.py, tests/__init__.py, tests/conftest.py</files>
  <behavior>
    - pyproject.toml has name="automl", requires-python=">=3.11", and dependencies: scikit-learn, pandas, numpy, xgboost, lightgbm, pytest
    - `uv sync` installs all dependencies without error
    - conftest.py provides fixtures: `sample_classification_csv` (tmp CSV with 200 rows, 5 numeric + 2 categorical columns, binary target), `sample_regression_csv` (tmp CSV with 200 rows, numeric target), `sample_csv_with_missing` (CSV with ~10% missing values in numeric and categorical columns)
    - All fixtures return a Path to the temp CSV file
  </behavior>
  <action>
    1. Create `pyproject.toml` with:
       - name = "automl", version = "0.1.0", requires-python = ">=3.11"
       - dependencies: scikit-learn>=1.5, pandas>=2.0, numpy>=2.0, xgboost, lightgbm
       - [tool.pytest.ini_options] testpaths = ["tests"]
       - dev dependencies: pytest
    2. Create `src/automl/__init__.py` (empty, makes it a package)
    3. Create `tests/__init__.py` (empty)
    4. Create `tests/conftest.py` with:
       - `sample_classification_csv(tmp_path)` fixture: generates a DataFrame with columns [feat_num1..feat_num5, feat_cat1, feat_cat2, target] where target is binary (0/1), writes to tmp_path/"data.csv", returns Path
       - `sample_regression_csv(tmp_path)` fixture: same numeric features, target is continuous float, returns Path
       - `sample_csv_with_missing(tmp_path)` fixture: classification CSV but with ~10% values set to NaN (using numpy), returns Path
       - Use numpy random seed 42 for reproducibility in all fixtures
    5. Run `uv sync` to install everything
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv sync && uv run python -c "import sklearn, pandas, numpy, xgboost, lightgbm; print('OK')" && uv run pytest tests/conftest.py --collect-only -q</automated>
  </verify>
  <done>pyproject.toml exists with all dependencies, `uv sync` succeeds, conftest.py fixtures are collectible by pytest</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prepare.py frozen data pipeline with tests</name>
  <files>src/automl/prepare.py, tests/test_prepare.py</files>
  <behavior>
    - test_load_data: load_data(csv_path, "target") returns (X: DataFrame, y: Series, task: str); task is "classification" for binary target, "regression" for continuous; X has no "target" column
    - test_load_data_int_classification: Integer target with <=20 unique values detected as "classification"
    - test_split_data: split_data(X, y, "classification") returns (X_work, X_holdout, y_work, y_holdout); holdout is ~15% of data; stratification preserved for classification
    - test_evaluate: evaluate(LogisticRegression(max_iter=1000), X_processed, y, "accuracy", "classification") returns (mean: float, std: float); mean is between 0 and 1
    - test_evaluate_regression: evaluate(Ridge(), X, y, "r2", "regression") returns (mean, std)
    - test_baselines: get_baselines(X_processed, y, "accuracy", "classification") returns dict with "most_frequent" and "stratified" keys, each having "score" and "std"
    - test_baselines_regression: get_baselines returns "mean" and "median" keys for regression
    - test_preprocess: build_preprocessor(X_train) returns a fitted ColumnTransformer; preprocessor.transform(X_test) produces numpy array with no NaN values
    - test_preprocess_categorical: Categorical columns are ordinal-encoded (not one-hot)
    - test_data_summary: get_data_summary(X, y, "classification") returns dict with keys: shape, dtypes, missing, target_distribution
    - test_data_summary_regression: target_distribution has mean, std, min, max for regression
    - test_validate_metric: validate_metric("auc", "classification") returns ("roc_auc", "maximize"); validate_metric("rmse", "regression") returns ("neg_root_mean_squared_error", "maximize")
    - test_validate_metric_mismatch: validate_metric("auc", "regression") raises ValueError
    - test_holdout_split: Holdout data is separate from working data (no index overlap)
  </behavior>
  <action>
    Write tests FIRST in tests/test_prepare.py (RED phase), then implement src/automl/prepare.py (GREEN phase).

    **tests/test_prepare.py** must import `from automl.prepare import load_data, split_data, evaluate, get_baselines, build_preprocessor, get_data_summary, validate_metric, METRIC_MAP` and test all behaviors listed above. Use the conftest fixtures.

    **src/automl/prepare.py** must implement:

    1. `METRIC_MAP` dict: Maps user-facing names (auc, rmse, f1, accuracy, mae, r2, mse, log_loss, f1_weighted, precision, recall) to (sklearn_scoring_string, direction) tuples. All directions are "maximize" because sklearn negates error metrics.

    2. `validate_metric(metric_name, task)`: Validate metric exists in METRIC_MAP and is compatible with task type. Return (sklearn_scoring, direction). Raise ValueError on unknown metric or task mismatch. Classification metrics: accuracy, auc, roc_auc, f1, f1_weighted, precision, recall, log_loss. Regression metrics: rmse, mae, r2, mse.

    3. `load_data(csv_path, target_column)`: Read CSV with pandas. Extract target Series. Drop target from features. Infer task: "classification" if target dtype is object/category OR if integer-typed with <=20 unique values; otherwise "regression". Return (X, y, task).

    4. `split_data(X, y, task, holdout_fraction=0.15, random_state=42)`: Use train_test_split. Stratify if classification (stratify=y), no stratify for regression. Return (X_work, X_holdout, y_work, y_holdout).

    5. `build_preprocessor(X)`: Use ColumnTransformer with:
       - Numeric columns: Pipeline([SimpleImputer(strategy="median")])
       - Categorical columns: Pipeline([SimpleImputer(strategy="most_frequent"), OrdinalEncoder(handle_unknown="use_encoded_value", unknown_value=-1)])
       - remainder="drop"
       Return the fitted preprocessor. Note: The function should call fit(X) internally and return the fitted transformer.

    6. `evaluate(model, X, y, metric, task, cv=5)`: Use cross_val_score with scoring=metric (the sklearn scoring string, NOT the user-facing name). For classification, use StratifiedKFold; for regression, use KFold. Return (scores.mean(), scores.std()).

    7. `get_baselines(X, y, metric, task)`: For classification: compute DummyClassifier with strategies "most_frequent" and "stratified". For regression: DummyRegressor with "mean" and "median". Use evaluate() for each. Return dict of {strategy: {"score": mean, "std": std}}.

    8. `get_data_summary(X, y, task)`: Return dict with shape (tuple), dtypes (value_counts dict), missing (int total missing values), target_distribution (value_counts dict for classification; {mean, std, min, max} for regression).

    Use imports from research: sklearn.model_selection (cross_val_score, StratifiedKFold, KFold, train_test_split), sklearn.dummy (DummyClassifier, DummyRegressor), sklearn.compose (ColumnTransformer), sklearn.pipeline (Pipeline), sklearn.impute (SimpleImputer), sklearn.preprocessing (OrdinalEncoder).
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_prepare.py -v -x</automated>
  </verify>
  <done>All tests in test_prepare.py pass. prepare.py exports all 8 public symbols. Classification and regression paths both work. Preprocessing handles missing values and categoricals without leakage. Metric validation catches task mismatches.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_prepare.py -v` -- all tests pass
- `uv run python -c "from automl.prepare import load_data, split_data, evaluate, get_baselines, build_preprocessor, get_data_summary, validate_metric, METRIC_MAP; print('All exports OK')"` -- all symbols importable
</verification>

<success_criteria>
- pyproject.toml exists with all dependencies and `uv sync` succeeds
- src/automl/prepare.py exists with all PIPE-* functions implemented
- tests/conftest.py provides reusable CSV fixtures
- tests/test_prepare.py passes with all 14+ test cases covering PIPE-01 through PIPE-07
- No data leakage in preprocessing (fit on train only)
- Metric validation catches classification/regression mismatches
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
