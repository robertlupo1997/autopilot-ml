---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automl/git_ops.py
  - src/automl/experiment_logger.py
  - tests/test_git.py
  - tests/test_logging.py
autonomous: true
requirements:
  - GIT-01
  - GIT-02
  - GIT-03
  - GIT-04
  - GIT-05
  - LOG-01
  - LOG-02
  - LOG-03

must_haves:
  truths:
    - "GitManager creates a dedicated experiment branch from current HEAD"
    - "GitManager commits specified files with descriptive messages and returns the commit hash"
    - "GitManager reverts to last commit (hard reset) on failure"
    - "All git operations use subprocess, never GitPython"
    - "results.tsv is created with correct TSV header on init"
    - "log_result appends tab-separated rows without overwriting previous entries"
    - "results.tsv fields include: commit, metric_value, memory_mb, elapsed_sec, status, description"
    - "run.log captures experiment stdout/stderr"
  artifacts:
    - path: "src/automl/git_ops.py"
      provides: "Git state management via subprocess"
      exports: ["GitManager"]
      min_lines: 60
    - path: "src/automl/experiment_logger.py"
      provides: "TSV logging and run.log management"
      exports: ["ExperimentLogger"]
      min_lines: 40
    - path: "tests/test_git.py"
      provides: "Integration tests for git operations"
      min_lines: 80
    - path: "tests/test_logging.py"
      provides: "Unit tests for TSV logging"
      min_lines: 60
  key_links:
    - from: "src/automl/git_ops.py"
      to: "subprocess"
      via: "subprocess.run(['git', ...])"
      pattern: "subprocess\\.run.*git"
    - from: "src/automl/experiment_logger.py"
      to: "results.tsv"
      via: "open() append mode"
      pattern: 'open.*results\\.tsv.*"a"'
---

<objective>
Build the git state management module and experiment logging module.

Purpose: These two utility modules handle experiment lifecycle -- git_ops.py manages branches/commits/reverts, and experiment_logger.py manages the TSV log and run.log capture. They are independent of the data pipeline and can be built in parallel with Plan 01.

Output: Working `src/automl/git_ops.py` (GitManager class) and `src/automl/experiment_logger.py` (ExperimentLogger class) with full test coverage.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Git operations module with integration tests</name>
  <files>src/automl/git_ops.py, tests/test_git.py</files>
  <behavior>
    - test_create_branch: GitManager.create_branch("test-run") creates branch "automl/run-test-run" and checks it out; verify with `git branch --show-current`
    - test_commit: GitManager.commit("test message", files=["test.txt"]) stages the file, commits, and returns a short hash (7+ hex chars)
    - test_revert: After modifying a tracked file, GitManager.revert() restores it to the last committed state
    - test_revert_preserves_untracked_gitignored: After revert, files in .gitignore (like results.tsv) are NOT deleted
    - test_gitignore: GitManager.init_repo() creates .gitignore containing results.tsv, run.log, __pycache__/, *.pyc
    - test_get_current_commit: Returns a valid short hash string
    - test_no_gitpython: No import of "git" or "gitpython" anywhere in git_ops.py (GIT-05)
    - test_subprocess_used: git_ops.py uses subprocess.run for all git commands
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    **tests/test_git.py**: Each test creates a temp directory (use tmp_path fixture), initializes a git repo there, and tests GitManager operations. Tests must:
    - Create a temp dir, init git, create an initial commit (so HEAD exists)
    - Test branch creation, commit, revert, .gitignore generation
    - For revert test: create a file, commit it, modify it, revert, verify original content
    - For gitignore test: verify results.tsv and run.log are in .gitignore content
    - For no-gitpython test: read git_ops.py source and assert "import git" not in it (except "import git_ops" style)

    **src/automl/git_ops.py**: Implement GitManager class:

    ```
    class GitManager:
        def __init__(self, repo_dir="."):
            self.repo_dir = repo_dir

        def _run(self, *args, check=True):
            """Run a git command via subprocess. Return CompletedProcess."""
            result = subprocess.run(
                ["git"] + list(args),
                capture_output=True, text=True, check=check,
                cwd=self.repo_dir
            )
            return result

        def init_repo(self):
            """Initialize git repo and create .gitignore if not exists."""
            if not os.path.exists(os.path.join(self.repo_dir, ".git")):
                self._run("init")
            gitignore_path = os.path.join(self.repo_dir, ".gitignore")
            if not os.path.exists(gitignore_path):
                with open(gitignore_path, "w") as f:
                    f.write("results.tsv\nrun.log\n__pycache__/\n*.pyc\n")
                self._run("add", ".gitignore")
                self._run("commit", "-m", "Initial commit with .gitignore")

        def create_branch(self, tag):
            """Create and checkout experiment branch. Returns branch name."""
            branch = f"automl/run-{tag}"
            self._run("checkout", "-b", branch)
            return branch

        def commit(self, message, files=None):
            """Stage files and commit. Returns short hash."""
            files = files or ["train.py"]
            for f in files:
                self._run("add", f)
            self._run("commit", "-m", message)
            result = self._run("rev-parse", "--short", "HEAD")
            return result.stdout.strip()

        def revert(self):
            """Hard reset to last commit. Does NOT touch untracked/ignored files."""
            self._run("reset", "--hard", "HEAD")

        def get_current_commit(self):
            """Return short hash of HEAD."""
            result = self._run("rev-parse", "--short", "HEAD")
            return result.stdout.strip()
    ```

    IMPORTANT: Do NOT use `git clean` in revert() -- it would delete untracked files. `git reset --hard HEAD` is sufficient because train.py is the only mutable tracked file.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_git.py -v -x</automated>
  </verify>
  <done>All git integration tests pass. GitManager creates branches, commits, reverts, and generates .gitignore. No GitPython dependency. All operations use subprocess.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Experiment logging module with tests</name>
  <files>src/automl/experiment_logger.py, tests/test_logging.py</files>
  <behavior>
    - test_init_creates_header: ExperimentLogger(dir).init_results() creates results.tsv with header "commit\tmetric_value\tmemory_mb\telapsed_sec\tstatus\tdescription\n"
    - test_log_result_appends: After logging 2 results, file has header + 2 data rows (3 lines total)
    - test_tsv_format: Each logged row is tab-separated with 6 fields
    - test_append_only: Logging does not overwrite previous entries
    - test_log_fields: Logged row contains correct commit, metric_value (6 decimal places), memory_mb (1 decimal), elapsed_sec (1 decimal), status, description
    - test_init_idempotent: Calling init_results() twice does not duplicate the header
    - test_write_run_log: ExperimentLogger.write_run_log(stdout, stderr) writes combined output to run.log
    - test_run_log_overwritten: Each call to write_run_log overwrites previous content (not append)
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    **tests/test_logging.py**: Use tmp_path fixture for each test. Create ExperimentLogger pointing at tmp_path. Test all behaviors above.

    **src/automl/experiment_logger.py**: Implement ExperimentLogger class:

    ```
    RESULTS_FILE = "results.tsv"
    RUN_LOG_FILE = "run.log"
    HEADER = "commit\tmetric_value\tmemory_mb\telapsed_sec\tstatus\tdescription\n"

    class ExperimentLogger:
        def __init__(self, experiment_dir="."):
            self.experiment_dir = experiment_dir
            self.results_path = os.path.join(experiment_dir, RESULTS_FILE)
            self.run_log_path = os.path.join(experiment_dir, RUN_LOG_FILE)

        def init_results(self):
            """Create results.tsv with header if it doesn't exist."""
            if not os.path.exists(self.results_path):
                with open(self.results_path, "w") as f:
                    f.write(HEADER)

        def log_result(self, commit, metric_value, memory_mb, elapsed_sec, status, description):
            """Append one experiment result row."""
            with open(self.results_path, "a") as f:
                f.write(f"{commit}\t{metric_value:.6f}\t{memory_mb:.1f}\t{elapsed_sec:.1f}\t{status}\t{description}\n")

        def write_run_log(self, stdout, stderr=""):
            """Write experiment output to run.log (overwrites)."""
            with open(self.run_log_path, "w") as f:
                f.write(stdout)
                if stderr:
                    f.write("\n--- STDERR ---\n")
                    f.write(stderr)
    ```

    Status values used by the framework: "keep", "discard", "crash", "draft-keep", "draft-discard".
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_logging.py -v -x</automated>
  </verify>
  <done>All logging tests pass. results.tsv is TSV with correct header and fields. Append-only behavior verified. run.log is overwritten per experiment. Init is idempotent.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_git.py tests/test_logging.py -v` -- all tests pass
- `grep -r "import git" src/automl/git_ops.py` -- returns nothing (no GitPython)
- `uv run python -c "from automl.git_ops import GitManager; from automl.experiment_logger import ExperimentLogger; print('OK')"` -- imports work
</verification>

<success_criteria>
- GitManager handles branch creation, commit, revert, and .gitignore setup via subprocess only
- ExperimentLogger creates TSV with correct header, appends rows, writes run.log
- All git tests pass using real temp git repos
- All logging tests pass using temp directories
- No GitPython dependency anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
