---
phase: 02-core-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automl/drafts.py
  - tests/test_drafts.py
autonomous: true
requirements: [DRAFT-01, DRAFT-02, DRAFT-03, DRAFT-04]

must_haves:
  truths:
    - "ALGORITHM_FAMILIES contains 5 classification algorithms and 5 regression algorithms"
    - "generate_draft_train_py swaps the model section in train_template.py for a given algorithm"
    - "select_best_draft picks the draft with highest metric_value"
    - "Draft results use status 'draft-keep' for winner and 'draft-discard' for others"
    - "Drafts cover diverse algorithm families: linear, tree, boosted, SVM/ElasticNet"
  artifacts:
    - path: "src/automl/drafts.py"
      provides: "ALGORITHM_FAMILIES, DraftResult, generate_draft_train_py, select_best_draft"
      exports: ["ALGORITHM_FAMILIES", "DraftResult", "generate_draft_train_py", "select_best_draft"]
    - path: "tests/test_drafts.py"
      provides: "Tests for draft generation, selection, and logging status"
      min_lines: 80
  key_links:
    - from: "src/automl/drafts.py"
      to: "src/automl/train_template.py"
      via: "Reads template and swaps model section"
      pattern: "# --- Model.*?---"
    - from: "src/automl/drafts.py"
      to: "CLAUDE.md draft phase (Plan 03)"
      via: "Agent calls generate_draft_train_py and select_best_draft during Phase 1 of loop"
      pattern: "generate_draft_train_py|select_best_draft"
---

<objective>
Build the multi-draft initialization system: algorithm family definitions, train.py variant generation, and best-draft selection logic.

Purpose: Before iterating, the agent generates 3-5 diverse train.py variants using different algorithm families, evaluates all of them, and selects the best as the starting point. This avoids getting stuck in a local optimum from a single algorithm choice.

Output: drafts.py with algorithm families, draft generation, and selection; comprehensive tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop/02-RESEARCH.md

<interfaces>
<!-- The train_template.py that drafts modify -->

From src/automl/train_template.py (lines 32-34, the mutable model section):
```python
# --- Model (agent edits this section) ---
from sklearn.linear_model import LogisticRegression
model = LogisticRegression(max_iter=1000)
```

The template has markers:
- `# --- Model (agent edits this section) ---` (start)
- `# --- Evaluate ---` (end boundary)

Drafts replace content BETWEEN these markers with the algorithm's imports + model instantiation.

From src/automl/runner.py:
```python
@dataclass
class ExperimentResult:
    metric_value: Optional[float]
    status: str  # "success", "crash", "timeout"
```

From src/automl/experiment_logger.py:
```python
class ExperimentLogger:
    def log_result(self, commit, metric_value, memory_mb, elapsed_sec, status, description):
    # status accepts any string including "draft-keep" and "draft-discard"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Draft generation and algorithm families with TDD</name>
  <files>src/automl/drafts.py, tests/test_drafts.py</files>
  <behavior>
    - ALGORITHM_FAMILIES["classification"] has 5 entries (LogisticRegression, RandomForest, XGBoost, LightGBM, SVM)
    - ALGORITHM_FAMILIES["regression"] has 5 entries (Ridge, RandomForest, XGBoost, LightGBM, ElasticNet)
    - Each algorithm dict has keys: "name", "imports", "model_line"
    - generate_draft_train_py reads template content and swaps model section
    - Generated draft contains the algorithm's import and model line
    - Generated draft preserves everything outside the model section (prepare import, evaluate call, structured output)
    - DraftResult dataclass has fields: name, metric_value, status, commit_hash, description
  </behavior>
  <action>
    RED: Create tests/test_drafts.py with:
    - test_classification_families: verify 5 entries, each has name/imports/model_line keys
    - test_regression_families: verify 5 entries, each has name/imports/model_line keys
    - test_generate_draft_replaces_model: create a temporary file with the train_template.py content, call generate_draft_train_py with an XGBoost algorithm dict, verify the output contains "XGBClassifier" and does NOT contain "LogisticRegression"
    - test_generate_draft_preserves_structure: verify the output still contains "from prepare import", "evaluate(", and "metric_value:"
    - test_draft_result_dataclass: verify DraftResult fields

    Run tests -- must FAIL.

    GREEN: Create src/automl/drafts.py implementing:

    ALGORITHM_FAMILIES dict (keyed by "classification" and "regression") exactly as specified in 02-RESEARCH.md Pattern 1. Each family has 5 algorithm dicts with name, imports, model_line.

    generate_draft_train_py(template_content: str, algorithm: dict) -> str:
    - Takes the template CONTENT (string, not path) and an algorithm dict
    - Uses regex to find the model section between "# --- Model" marker and "# --- Evaluate" marker
    - Replaces the content between markers with algorithm["imports"] + "\n" + algorithm["model_line"]
    - Returns the modified content string
    - IMPORTANT: Takes content string, not file path. The caller reads the file. This makes testing easy.

    DraftResult dataclass with fields: name (str), metric_value (float|None), status (str), commit_hash (str), description (str).

    select_best_draft(results: list[DraftResult]) -> DraftResult | None:
    - Returns the DraftResult with highest metric_value among those with metric_value not None
    - Returns None if no successful drafts
    - This function does NOT set statuses -- the caller marks winner as "draft-keep" and others as "draft-discard"
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_drafts.py -x -v</automated>
  </verify>
  <done>drafts.py exports ALGORITHM_FAMILIES, DraftResult, generate_draft_train_py, select_best_draft. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Draft selection and logging status tests</name>
  <files>src/automl/drafts.py, tests/test_drafts.py</files>
  <behavior>
    - select_best_draft picks highest metric_value from list of DraftResults
    - select_best_draft returns None for empty list
    - select_best_draft ignores drafts with metric_value=None (crashes)
    - select_best_draft handles all-None results (returns None)
    - DraftResult status field accepts "draft-keep" and "draft-discard" strings
  </behavior>
  <action>
    RED: Add to tests/test_drafts.py:
    - test_select_best_draft: create 3 DraftResults with metric_values [0.75, 0.90, 0.82], verify select_best_draft returns the one with 0.90
    - test_select_best_draft_empty: verify returns None for empty list
    - test_select_best_draft_all_crashed: create DraftResults with all metric_value=None, verify returns None
    - test_select_best_draft_some_crashed: mix of None and valid metric_values, verify picks best valid
    - test_draft_status_strings: create DraftResult with status="draft-keep" and "draft-discard", verify they hold

    GREEN: Implement select_best_draft in drafts.py if not already complete from Task 1. Filter out results where metric_value is None, return max by metric_value, or None if empty after filtering.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_drafts.py -x -v</automated>
  </verify>
  <done>select_best_draft correctly handles all edge cases. Draft status strings work. All draft tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/tlupo/AutoML && uv run pytest tests/test_drafts.py -v
```
All draft tests green. No regressions:
```bash
cd /home/tlupo/AutoML && uv run pytest tests/ -x -q
```
</verification>

<success_criteria>
- drafts.py exports ALGORITHM_FAMILIES (5 classification + 5 regression algorithms), DraftResult, generate_draft_train_py, select_best_draft
- generate_draft_train_py correctly swaps model section in template while preserving structure
- select_best_draft handles normal, empty, all-crashed, and mixed scenarios
- All tests pass, zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop/02-02-SUMMARY.md`
</output>
