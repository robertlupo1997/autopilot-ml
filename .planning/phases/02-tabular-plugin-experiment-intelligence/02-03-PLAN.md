---
phase: 02-tabular-plugin-experiment-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/mlforge/journal.py
  - src/mlforge/results.py
  - tests/mlforge/test_journal.py
  - tests/mlforge/test_results.py
autonomous: true
requirements: [INTL-06, INTL-08]

must_haves:
  truths:
    - "Journal entries can include a diff field showing git diff between experiments"
    - "Diff-aware journal entries show the agent what code changed between experiments"
    - "Results tracking provides structured experiment log with commit hash, metric value, status, description, timestamp"
    - "Results can be loaded, queried by status, and summarized"
  artifacts:
    - path: "src/mlforge/journal.py"
      provides: "Enhanced journal with diff field support"
      exports: ["JournalEntry", "append_journal_entry", "load_journal", "render_journal_markdown"]
    - path: "src/mlforge/results.py"
      provides: "Structured results tracking and querying"
      exports: ["ExperimentResult", "ResultsTracker"]
    - path: "tests/mlforge/test_journal.py"
      provides: "Tests for diff-aware journal entries"
      min_lines: 30
    - path: "tests/mlforge/test_results.py"
      provides: "Tests for results tracking, querying, and summarization"
      min_lines: 40
  key_links:
    - from: "src/mlforge/journal.py"
      to: "src/mlforge/git_ops.py"
      via: "GitManager.repo.git.diff for generating diffs"
      pattern: "git\\.diff|diff"
    - from: "src/mlforge/results.py"
      to: "src/mlforge/journal.py"
      via: "Results complement journal entries with structured querying"
      pattern: "ExperimentResult|ResultsTracker"
---

<objective>
Wire diff-aware iteration into the journal and create structured results tracking, completing the experiment intelligence subsystem.

Purpose: Diff-aware journal entries show the agent what changed between experiments (eliminating repeated approaches), and structured results tracking provides queryable experiment history with commit hashes and timestamps.
Output: Enhanced journal.py with diff support and new results.py module with full test coverage.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tabular-plugin-experiment-intelligence/02-RESEARCH.md

<interfaces>
<!-- Existing journal interface being enhanced -->

From src/mlforge/journal.py:
```python
@dataclass
class JournalEntry:
    experiment_id: int
    hypothesis: str
    result: str
    metric_value: float | None
    metric_delta: float | None
    commit_hash: str | None
    status: str  # "keep" | "revert" | "crash"

def append_journal_entry(path: Path, entry: JournalEntry) -> None: ...
def load_journal(path: Path) -> list[dict]: ...
def render_journal_markdown(entries: list[dict]) -> str: ...
```

From src/mlforge/git_ops.py:
```python
class GitManager:
    repo: Repo  # GitPython Repo instance
    # repo.git.diff("HEAD~1") returns diff string
```

From src/mlforge/state.py:
```python
@dataclass
class SessionState:
    experiment_count: int = 0
    best_metric: float | None = None
    best_commit: str | None = None
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diff-aware journal enhancement</name>
  <files>
    src/mlforge/journal.py,
    tests/mlforge/test_journal.py
  </files>
  <behavior>
    - JournalEntry dataclass gains optional diff field: diff: str | None = None
    - append_journal_entry serializes diff field to JSONL when present
    - load_journal returns entries with diff field included
    - render_journal_markdown includes truncated diff (first 500 chars) when present
    - get_last_diff(repo_path) returns git diff HEAD~1 string, or None if no previous commit
    - Existing tests continue to pass (backward compatible -- diff defaults to None)
  </behavior>
  <action>
    1. Read existing tests/mlforge/test_journal.py to understand current test patterns.

    2. Add new tests FIRST to tests/mlforge/test_journal.py:
    - test_journal_entry_diff_field_default: diff defaults to None
    - test_journal_entry_with_diff: entry with diff string serializes correctly
    - test_append_and_load_with_diff: round-trip JSONL with diff field
    - test_render_markdown_with_diff: diff shown (truncated) in markdown output
    - test_get_last_diff_with_commits: returns diff string from git repo (use tmp git fixture)
    - test_get_last_diff_no_previous: returns None when only one commit exists

    3. Enhance src/mlforge/journal.py:
    - Add diff: str | None = None to JournalEntry dataclass (defaults maintain backward compat)
    - append_journal_entry: no changes needed (asdict already handles new field)
    - render_journal_markdown: if entry has non-None "diff" key, append a collapsed diff section below the table row (or add a "Diff" column with truncated preview)
    - Add get_last_diff(repo_path: Path | str) -> str | None function: opens GitPython Repo, tries repo.git.diff("HEAD~1"), returns diff string or None on GitCommandError (no previous commit)

    Backward compatibility: existing entries without "diff" key still load correctly (load_journal uses JSON dicts, new field just absent).
  </action>
  <verify>
    <automated>python3 -m pytest tests/mlforge/test_journal.py -x -v</automated>
  </verify>
  <done>
    - JournalEntry has optional diff field defaulting to None
    - Diff content persists through JSONL round-trip
    - get_last_diff returns git diff string from repo
    - All existing + new journal tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured results tracking module</name>
  <files>
    src/mlforge/results.py,
    tests/mlforge/test_results.py
  </files>
  <behavior>
    - ExperimentResult dataclass: experiment_id (int), commit_hash (str|None), metric_name (str), metric_value (float|None), status (str), description (str), timestamp (str)
    - ResultsTracker manages a list of ExperimentResult and persists to JSONL
    - ResultsTracker.add(result) appends to in-memory list and persists to file
    - ResultsTracker.load(path) reads JSONL file into ExperimentResult list
    - ResultsTracker.get_best(direction) returns ExperimentResult with best metric
    - ResultsTracker.get_by_status(status) returns filtered list
    - ResultsTracker.summary() returns dict with total_experiments, keeps, reverts, best_metric, best_commit
    - Empty tracker returns None for get_best and empty lists for get_by_status
  </behavior>
  <action>
    1. Write tests FIRST in tests/mlforge/test_results.py:
    - test_experiment_result_dataclass: all fields present and typed correctly
    - test_tracker_add_and_load: round-trip JSONL persistence
    - test_tracker_get_best_maximize: returns result with highest metric
    - test_tracker_get_best_minimize: returns result with lowest metric
    - test_tracker_get_best_empty: returns None for empty tracker
    - test_tracker_get_best_all_none_metrics: returns None when all metrics are None
    - test_tracker_get_by_status: filters correctly by keep/revert/crash
    - test_tracker_summary: correct counts and best metric/commit
    - test_tracker_summary_empty: handles empty tracker gracefully

    2. Create src/mlforge/results.py:
    - ExperimentResult dataclass with fields: experiment_id (int), commit_hash (str|None), metric_name (str), metric_value (float|None), status (str), description (str), timestamp (str)
    - ResultsTracker class:
      - __init__(path: Path): stores path, loads existing results if file exists
      - results: list[ExperimentResult] property
      - add(result: ExperimentResult): append to list, write line to JSONL file
      - load(path: Path) -> ResultsTracker (classmethod): create tracker from existing file
      - get_best(direction: str = "maximize") -> ExperimentResult | None: filter non-None metrics, return max/min by direction
      - get_by_status(status: str) -> list[ExperimentResult]: filter by status field
      - summary() -> dict: total_experiments, keeps (status=="keep"), reverts (status=="revert"), crashes (status=="crash"), best_metric (value), best_commit (hash)
  </action>
  <verify>
    <automated>python3 -m pytest tests/mlforge/test_results.py -x -v</automated>
  </verify>
  <done>
    - ExperimentResult captures all required fields (commit hash, metric, status, timestamp)
    - ResultsTracker persists to JSONL and loads correctly
    - get_best handles both maximize/minimize directions and edge cases
    - get_by_status filters correctly
    - summary provides complete experiment statistics
    - All tests pass
  </done>
</task>

</tasks>

<verification>
python3 -m pytest tests/mlforge/ -x -q
</verification>

<success_criteria>
- Diff-aware journal entries capture git diffs between experiments
- Structured results tracking provides queryable experiment history
- Both modules are backward compatible with Phase 1 data formats
- All new + existing tests pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tabular-plugin-experiment-intelligence/02-03-SUMMARY.md`
</output>
