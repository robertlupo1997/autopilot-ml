---
phase: 03-cli-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automl/scaffold.py
  - tests/test_scaffold.py
autonomous: true
requirements:
  - CLI-01
  - CLI-03
  - CLI-04

must_haves:
  truths:
    - "scaffold_experiment() creates a complete experiment directory with all 7 files"
    - "Generated prepare.py is identical to the installed automl.prepare source"
    - "Generated train.py has correct CSV_PATH, TARGET_COLUMN, METRIC, TIME_BUDGET values"
    - "Generated program.md contains real data summary and baselines (not placeholders)"
    - "Generated pyproject.toml lists scikit-learn, pandas, numpy, xgboost, lightgbm as dependencies"
    - "scaffold_experiment() fails with FileExistsError if output dir already exists"
  artifacts:
    - path: "src/automl/scaffold.py"
      provides: "Experiment directory scaffolding"
      exports: ["scaffold_experiment"]
    - path: "tests/test_scaffold.py"
      provides: "Scaffold integration tests"
      min_lines: 80
  key_links:
    - from: "src/automl/scaffold.py"
      to: "automl.prepare"
      via: "inspect.getfile + shutil.copy2"
      pattern: "inspect\\.getfile.*prepare"
    - from: "src/automl/scaffold.py"
      to: "automl.templates"
      via: "render_program_md, render_claude_md"
      pattern: "render_program_md|render_claude_md"
    - from: "src/automl/scaffold.py"
      to: "automl.train_template"
      via: "inspect.getfile to read template content"
      pattern: "inspect\\.getfile.*train_template"
---

<objective>
Create the scaffold module that generates complete standalone experiment directories from a CSV file.

Purpose: This is the core of Phase 3 -- the function that composes all Phase 1/2 modules into a single "CSV to experiment project" operation. The scaffold module validates inputs, copies prepare.py, generates train.py with correct config, computes data summary and baselines for program.md, and writes all supporting files.

Output: src/automl/scaffold.py with scaffold_experiment() function, plus comprehensive integration tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-and-integration/03-RESEARCH.md

@src/automl/prepare.py
@src/automl/train_template.py
@src/automl/templates/__init__.py
@tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/automl/prepare.py:
```python
def load_data(csv_path: str | Path, target_column: str) -> tuple[pd.DataFrame, pd.Series, str]:
def validate_metric(metric_name: str, task: str) -> tuple[str, str]:
def build_preprocessor(X: pd.DataFrame) -> ColumnTransformer:
def get_baselines(X, y, metric: str, task: str) -> dict[str, dict[str, float]]:
def get_data_summary(X: pd.DataFrame, y: pd.Series, task: str) -> dict[str, Any]:
```

From src/automl/templates/__init__.py:
```python
def render_program_md(dataset_name: str, goal_description: str, metric_name: str, direction: str, data_summary: str = "", baselines: str = "") -> str:
def render_claude_md() -> str:
```

From src/automl/train_template.py (config section at top):
```python
CSV_PATH = "data.csv"
TARGET_COLUMN = "target"
METRIC = "accuracy"           # user-facing metric name
TIME_BUDGET = 60              # seconds
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scaffold tests</name>
  <files>tests/test_scaffold.py</files>
  <behavior>
    - test_scaffold_creates_all_files: scaffold_experiment() creates output dir containing prepare.py, train.py, program.md, CLAUDE.md, .gitignore, pyproject.toml, and the CSV copy (7 files total)
    - test_scaffold_prepare_py_matches_source: the copied prepare.py is byte-identical to inspect.getfile(automl.prepare)
    - test_scaffold_train_py_config: generated train.py has CSV_PATH matching the CSV filename, TARGET_COLUMN matching the provided target, METRIC matching the provided metric, TIME_BUDGET matching the provided budget
    - test_scaffold_program_md_has_data_summary: program.md contains dataset shape info and baseline scores (not placeholder text)
    - test_scaffold_pyproject_has_deps: pyproject.toml contains scikit-learn, pandas, numpy, xgboost, lightgbm
    - test_scaffold_gitignore: .gitignore lists results.tsv, run.log, __pycache__/, .venv/
    - test_scaffold_fails_if_dir_exists: raises FileExistsError when output_dir already exists
    - test_scaffold_default_output_dir: when output_dir=None, creates "experiment-{csv_stem}" directory
    - test_scaffold_csv_copied: CSV file is copied into the experiment directory
  </behavior>
  <action>
Create tests/test_scaffold.py with the tests above. Import scaffold_experiment from automl.scaffold (it will not exist yet -- RED phase). Use the sample_classification_csv fixture from conftest.py for all tests. Each test calls scaffold_experiment() with the fixture CSV, target_column="target", metric="accuracy", and asserts the specific file contents. For the "fails if dir exists" test, create the directory first with mkdir(). Run tests to confirm they fail (RED).
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_scaffold.py -x -q 2>&1 | head -20</automated>
  </verify>
  <done>test_scaffold.py exists with 9+ test functions, all failing with ImportError (scaffold module not yet created)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scaffold module</name>
  <files>src/automl/scaffold.py</files>
  <action>
Create src/automl/scaffold.py implementing scaffold_experiment() following the research architecture (Pattern 1). The function:

1. Resolves csv_path to absolute with Path(data_path).resolve(), raises FileNotFoundError if missing
2. Calls load_data(str(csv_path), target_column) and validate_metric(metric, task) for early validation
3. Creates output directory with mkdir(parents=True, exist_ok=False) -- fails if exists
4. Default output_dir: f"experiment-{csv_path.stem}"
5. Copies prepare.py via shutil.copy2(inspect.getfile(prepare_module), out / "prepare.py")
6. Generates train.py: reads train_template.py source via inspect.getfile(), replaces config lines using exact string matching:
   - 'CSV_PATH = "data.csv"' -> f'CSV_PATH = "{csv_path.name}"'
   - 'TARGET_COLUMN = "target"' -> f'TARGET_COLUMN = "{target_column}"'
   - 'METRIC = "accuracy"' -> f'METRIC = "{metric}"'
   - 'TIME_BUDGET = 60' -> f'TIME_BUDGET = {time_budget}'
7. Copies CSV into experiment dir with shutil.copy2
8. Computes data summary and baselines using build_preprocessor, get_baselines, get_data_summary, then formats them as strings for render_program_md
9. Renders program.md via render_program_md() with computed summary and baselines
10. Renders CLAUDE.md via render_claude_md()
11. Writes .gitignore with: results.tsv, run.log, __pycache__/, *.pyc, .venv/
12. Writes pyproject.toml with [project] section listing name=experiment-{stem}, dependencies=[scikit-learn>=1.5, pandas>=2.0, numpy>=2.0, xgboost, lightgbm], requires-python>=3.11
13. Returns the output Path

Helper functions: _format_summary(summary: dict) -> str, _format_baselines(baselines: dict) -> str, _gitignore_content() -> str, _pyproject_content(dataset_name: str) -> str

Per research: use textwrap.dedent for pyproject.toml generation. Do NOT add [build-system] to the generated pyproject.toml -- uv run script.py does not require it.

After implementation, run all scaffold tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_scaffold.py -x -v</automated>
  </verify>
  <done>All test_scaffold.py tests pass. scaffold_experiment() creates a complete 7-file experiment directory from any CSV.</done>
</task>

</tasks>

<verification>
- All tests in test_scaffold.py pass: `uv run pytest tests/test_scaffold.py -v`
- Existing test suite still passes: `uv run pytest tests/ -x -q`
- scaffold_experiment() is importable: `uv run python -c "from automl.scaffold import scaffold_experiment; print('OK')"`
</verification>

<success_criteria>
- scaffold_experiment() accepts (data_path, target_column, metric, goal, output_dir, time_budget) and returns Path to created directory
- Created directory contains all 7 files: prepare.py, train.py, program.md, CLAUDE.md, .gitignore, pyproject.toml, {data}.csv
- prepare.py is an exact copy of the installed source
- train.py has correctly substituted config values
- program.md has real data summary and baselines (not placeholders)
- pyproject.toml has all ML dependencies listed
- FileExistsError raised if output_dir already exists
- 9+ integration tests all green
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-and-integration/03-01-SUMMARY.md`
</output>
