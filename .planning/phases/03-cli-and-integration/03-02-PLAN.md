---
phase: 03-cli-and-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/automl/cli.py
  - pyproject.toml
  - tests/test_cli.py
  - tests/test_e2e.py
autonomous: true
requirements:
  - CLI-02
  - CLI-04

must_haves:
  truths:
    - "User can run `automl data.csv target_col metric` from the command line"
    - "CLI accepts --goal, --output-dir, --time-budget optional flags"
    - "CLI prints helpful error on missing/invalid arguments"
    - "CLI prints scaffold path and next-steps instructions on success"
    - "Scaffolded project produces valid structured metric output when run with uv run train.py"
  artifacts:
    - path: "src/automl/cli.py"
      provides: "CLI entry point"
      exports: ["main"]
    - path: "pyproject.toml"
      provides: "Entry point registration"
      contains: "[project.scripts]"
    - path: "tests/test_cli.py"
      provides: "CLI argument parsing tests"
      min_lines: 40
    - path: "tests/test_e2e.py"
      provides: "End-to-end scaffold + run test"
      min_lines: 30
  key_links:
    - from: "src/automl/cli.py"
      to: "automl.scaffold.scaffold_experiment"
      via: "import and call"
      pattern: "from automl\\.scaffold import scaffold_experiment"
    - from: "pyproject.toml"
      to: "automl.cli:main"
      via: "[project.scripts] entry point"
      pattern: "automl.*=.*automl\\.cli:main"
---

<objective>
Create the CLI entry point that wraps scaffold_experiment() with argparse, register it as a `[project.scripts]` entry point, and validate the full end-to-end flow.

Purpose: This is the user-facing interface -- the single command that takes a CSV and produces a ready-to-run experiment project. Plan 01 built the scaffold logic; this plan adds the CLI layer, registers the entry point, and proves the full pipeline works end-to-end.

Output: src/automl/cli.py, updated pyproject.toml with entry point, CLI tests, and an e2e test that scaffolds a project and runs train.py.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-and-integration/03-RESEARCH.md
@.planning/phases/03-cli-and-integration/03-01-SUMMARY.md

@src/automl/scaffold.py
@pyproject.toml
@tests/conftest.py

<interfaces>
<!-- From Plan 01 output -->

From src/automl/scaffold.py:
```python
def scaffold_experiment(
    data_path: str,
    target_column: str,
    metric: str,
    goal: str = "",
    output_dir: str | None = None,
    time_budget: int = 60,
) -> Path:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CLI module, entry point, and tests</name>
  <files>src/automl/cli.py, pyproject.toml, tests/test_cli.py</files>
  <behavior>
    - test_cli_help: main(["--help"]) exits 0 and prints usage info (capture with pytest.raises(SystemExit))
    - test_cli_missing_args: main([]) exits non-zero (missing required positional args)
    - test_cli_valid_args: main([csv_path, "target", "accuracy"]) exits 0 and creates experiment directory
    - test_cli_with_optional_flags: main([csv_path, "target", "accuracy", "--goal", "predict something", "--output-dir", dir, "--time-budget", "120"]) creates dir with correct settings
    - test_cli_bad_csv: main(["nonexistent.csv", "target", "accuracy"]) exits 1 and prints error to stderr
    - test_cli_bad_metric: main([csv_path, "target", "bogus_metric"]) exits 1 and prints error to stderr
  </behavior>
  <action>
1. Create src/automl/cli.py with main(argv=None) -> int function:
   - argparse.ArgumentParser with prog="automl", description="Scaffold an autonomous ML experiment from a CSV file."
   - Positional args: data_path, target_column, metric
   - Optional flags: --goal (default ""), --output-dir (default None), --time-budget (type=int, default=60)
   - try/except around scaffold_experiment() call, print error to stderr on failure, return 1
   - On success: print scaffold path and next-steps instructions (cd, uv run train.py, start Claude Code), return 0
   - Add `if __name__ == "__main__": sys.exit(main())` block

2. Update pyproject.toml: add `[project.scripts]` section with `automl = "automl.cli:main"`

3. Create tests/test_cli.py with the tests above. Use sample_classification_csv fixture. For tests that create directories, use tmp_path and pass --output-dir to control location. Test main() directly by passing argv list (not subprocess).

Run tests to confirm: RED for cli tests initially (cli.py not yet created), then GREEN after creating cli.py.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_cli.py -x -v</automated>
  </verify>
  <done>cli.py implements argparse with 3 positional + 3 optional args. Entry point registered in pyproject.toml. All CLI tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end test -- scaffold and run train.py</name>
  <files>tests/test_e2e.py</files>
  <behavior>
    - test_scaffolded_project_runs: scaffold a project from sample CSV, then run `uv run train.py` inside the scaffolded directory, verify it exits 0 and stdout contains the structured output lines (metric_name, metric_value, direction)
    - test_scaffolded_train_py_metrics_parseable: the structured output from train.py contains parseable float for metric_value and valid direction string
  </behavior>
  <action>
Create tests/test_e2e.py with integration tests that validate CLI-04 (generated project is immediately runnable). Use subprocess.run to execute `uv run train.py` inside the scaffolded experiment directory. This tests the FULL chain: scaffold -> generated pyproject.toml resolves deps -> prepare.py loads data -> train.py runs model -> structured output printed.

Key implementation details:
- Use sample_classification_csv fixture for the CSV source
- Call scaffold_experiment() directly (not via CLI) to create the experiment dir in tmp_path
- Run `uv run train.py` via subprocess.run with cwd=scaffolded_dir, capture_output=True, timeout=120
- Assert return code 0
- Assert stdout contains "metric_name:", "metric_value:", "direction:" lines
- Parse metric_value as float, assert it's a valid number
- Mark these tests with @pytest.mark.slow if they take >10s (uv venv creation)

Note: These tests will take longer (~15-30s) because uv needs to create a venv and install dependencies in the scaffolded project. Set subprocess timeout to 120s.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_e2e.py -x -v --timeout=120</automated>
  </verify>
  <done>E2E test passes: scaffolded project runs train.py successfully and produces valid structured metric output. CLI-04 verified.</done>
</task>

</tasks>

<verification>
- All CLI tests pass: `uv run pytest tests/test_cli.py -v`
- E2E test passes: `uv run pytest tests/test_e2e.py -v`
- Full suite still green: `uv run pytest tests/ -x -q`
- Entry point works: `uv run automl --help` prints usage
</verification>

<success_criteria>
- `automl data.csv target_col metric` scaffolds a project (via registered entry point)
- CLI accepts --goal, --output-dir, --time-budget optional flags
- CLI prints error message and exits 1 on invalid input
- Scaffolded project is immediately runnable: `uv run train.py` inside scaffolded dir exits 0
- train.py produces structured metric output with metric_name, metric_value, direction lines
- All tests green (test_cli.py + test_e2e.py + existing suite)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-and-integration/03-02-SUMMARY.md`
</output>
