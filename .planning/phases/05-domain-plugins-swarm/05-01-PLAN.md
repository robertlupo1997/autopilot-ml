---
phase: 05-domain-plugins-swarm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlforge/deeplearning/__init__.py
  - src/mlforge/deeplearning/prepare.py
  - src/mlforge/templates/dl_train.py.j2
  - tests/mlforge/test_dl_plugin.py
  - pyproject.toml
autonomous: true
requirements:
  - DL-01
  - DL-02
  - DL-03
  - DL-04
  - DL-05

must_haves:
  truths:
    - "DeepLearningPlugin registers with plugin registry and passes isinstance(DomainPlugin) check"
    - "scaffold() creates frozen prepare.py with GPU info reporting and image/text data loading"
    - "scaffold() renders mutable train.py from dl_train.py.j2 with time budget, LR scheduling, early stopping, gradient clipping"
    - "template_context() returns domain-specific DL rules for CLAUDE.md protocol"
    - "validate_config() rejects unknown DL metrics and missing required settings; accepts task=custom for custom architectures"
    - "All DL deps are lazy-imported -- importing mlforge.deeplearning does not import torch because the module uses in-method imports only; prepare.py uses module-level torch imports but is a standalone file copied during scaffold, never imported by mlforge core"
  artifacts:
    - path: "src/mlforge/deeplearning/__init__.py"
      provides: "DeepLearningPlugin class implementing DomainPlugin Protocol"
      exports: ["DeepLearningPlugin"]
    - path: "src/mlforge/deeplearning/prepare.py"
      provides: "Frozen data loader with GPU info, image/text loading"
      contains: "get_device_info"
    - path: "src/mlforge/templates/dl_train.py.j2"
      provides: "Jinja2 template for mutable train.py with time budget, early stopping"
      contains: "TIME_BUDGET_SEC"
    - path: "tests/mlforge/test_dl_plugin.py"
      provides: "Unit tests for DL-01 through DL-05"
      min_lines: 80
  key_links:
    - from: "src/mlforge/deeplearning/__init__.py"
      to: "src/mlforge/plugins.py"
      via: "DomainPlugin Protocol conformance"
      pattern: "class DeepLearningPlugin"
    - from: "src/mlforge/deeplearning/__init__.py"
      to: "src/mlforge/templates/dl_train.py.j2"
      via: "Jinja2 template rendering in scaffold()"
      pattern: "env.get_template.*dl_train"
---

<objective>
Build the deep learning plugin for mlforge, handling image classification, text classification, and custom architecture training with PyTorch.

Purpose: Extends mlforge from tabular-only to deep learning, proving the plugin architecture scales to GPU-based domains with time-budgeted training.
Output: DeepLearningPlugin class, frozen prepare.py, dl_train.py.j2 template, DL-specific CLAUDE.md rules, pyproject.toml optional deps for both DL and FT, and comprehensive tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-domain-plugins-swarm/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/mlforge/plugins.py:
```python
@runtime_checkable
class DomainPlugin(Protocol):
    name: str
    frozen_files: list[str]
    def scaffold(self, target_dir: Path, config: Config) -> None: ...
    def template_context(self, config: Config) -> dict: ...
    def validate_config(self, config: Config) -> list[str]: ...

def register_plugin(plugin: DomainPlugin) -> None: ...
def get_plugin(name: str) -> DomainPlugin: ...
```

From src/mlforge/config.py:
```python
@dataclass
class Config:
    domain: str = "tabular"
    metric: str = "accuracy"
    direction: str = "maximize"
    budget_minutes: int = 60
    plugin_settings: dict = field(default_factory=dict)
    frozen_files: list[str] = field(default_factory=lambda: ["prepare.py"])
    mutable_files: list[str] = field(default_factory=lambda: ["train.py"])
```

From src/mlforge/templates/__init__.py:
```python
def get_template_env() -> Environment: ...
def render_claude_md(plugin: DomainPlugin, config: Config) -> str: ...
```

From src/mlforge/tabular/__init__.py (reference implementation):
```python
class TabularPlugin:
    name: str = "tabular"
    frozen_files: list[str] = ["prepare.py"]
    def scaffold(self, target_dir: Path, config: Config) -> None: ...
    def template_context(self, config: Config) -> dict: ...
    def validate_config(self, config: Config) -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeepLearningPlugin class + plugin registry integration + tests</name>
  <files>src/mlforge/deeplearning/__init__.py, tests/mlforge/test_dl_plugin.py</files>
  <behavior>
    - Test: DeepLearningPlugin satisfies isinstance(DomainPlugin) check
    - Test: plugin.name == "deeplearning" and plugin.frozen_files == ["prepare.py"]
    - Test: scaffold() creates prepare.py and train.py in target directory
    - Test: validate_config() returns empty list for valid DL metrics (accuracy, f1, f1_weighted, loss)
    - Test: validate_config() returns error for unknown metric
    - Test: validate_config() accepts plugin_settings with task="image_classification", "text_classification", or "custom"
    - Test: validate_config() returns error for unknown task type (e.g. task="foo")
    - Test: template_context() returns dict with "domain_rules" list containing DL-specific rules
    - Test: template_context() rules mention timm, transformers, frozen prepare.py, mixed precision, early stopping, gradient clipping
    - Test: register_plugin(DeepLearningPlugin()) succeeds and get_plugin("deeplearning") returns it
    - Test: importing mlforge.deeplearning does NOT import torch -- verified by mocking sys.modules: insert a mock for "torch" that raises ImportError, then import mlforge.deeplearning successfully (because all torch usage is in-method, not module-level). This proves the lazy import pattern.
  </behavior>
  <action>
    1. Add `src/mlforge/deeplearning/__init__.py` with DeepLearningPlugin class following TabularPlugin pattern exactly:
       - `name = "deeplearning"`, `frozen_files = ["prepare.py"]`
       - `scaffold()`: Copy frozen prepare.py from deeplearning/prepare.py source; render mutable train.py from dl_train.py.j2 template. Use same `Path(__file__).parent / "prepare.py"` and `get_template_env().get_template("dl_train.py.j2")` pattern as TabularPlugin.
       - `template_context()`: Return domain_rules list with DL-specific rules:
         * Use timm pretrained models for image classification
         * Use transformers AutoModel for text classification
         * Do not modify prepare.py -- frozen infrastructure
         * train.py is the ONLY mutable file
         * Check GPU memory before loading model: torch.cuda.mem_get_info()
         * Use mixed precision (torch.amp) for memory efficiency
         * Stop training when time budget expires (check wall clock between epochs)
         * Use early stopping with patience=5 on validation loss
         * Apply gradient clipping: torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)
         * Use ReduceLROnPlateau scheduler for adaptive learning rate
       - `validate_config()`: Check metric in {"accuracy", "f1", "f1_weighted", "loss"}. Check plugin_settings.task in {"image_classification", "text_classification", "custom"} if provided -- "custom" is valid and means the user provides their own architecture via the mutable train.py (no template rendering for the model creation section). Return error for unrecognized task values.
       - ALL torch/timm/transformers imports MUST be inside methods, NOT at module level. This prevents ImportError when DL deps are not installed.

    2. Write initial `tests/mlforge/test_dl_plugin.py` with all behaviors listed above. For the lazy import check: use `unittest.mock.patch.dict("sys.modules", {"torch": None})` to block torch at module level, then verify `import mlforge.deeplearning` succeeds. This works because prepare.py is a standalone FILE that gets copied during scaffold -- it is never imported by mlforge core code. Module-level torch imports in prepare.py are fine because that file runs in the experiment directory, not as part of the mlforge package.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python3 -m pytest tests/mlforge/test_dl_plugin.py -x -q --timeout=30</automated>
  </verify>
  <done>DeepLearningPlugin passes all DomainPlugin Protocol checks, validates config including task=custom, template_context returns DL rules, lazy imports verified via sys.modules mock strategy</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frozen prepare.py + pyproject.toml optional deps (DL + FT groups)</name>
  <files>src/mlforge/deeplearning/prepare.py, pyproject.toml, tests/mlforge/test_dl_plugin.py</files>
  <behavior>
    - Test: prepare.py contains get_device_info(), load_image_data(), load_text_data() functions
    - Test: get_device_info() returns dict with keys "device", "gpu_name", "vram_gb" (mocked torch.cuda)
    - Test: load_image_data() returns train_loader, val_loader, num_classes (mocked torchvision)
    - Test: load_text_data() returns train_loader, val_loader, num_labels (mocked transformers)
    - Test: pyproject.toml has `dl` optional dependency group with torch, torchvision, timm, transformers, datasets
    - Test: pyproject.toml has `ft` optional dependency group with peft, trl, bitsandbytes, evaluate, rouge-score, transformers, datasets
  </behavior>
  <action>
    1. Add `src/mlforge/deeplearning/prepare.py` -- the frozen data loading module:
       - `get_device_info() -> dict`: Returns {"device": "cuda"/"cpu", "gpu_name": str|None, "vram_gb": float}. Uses torch.cuda lazily.
       - `load_image_data(data_dir, img_size=224, batch_size=32)`: Standard torchvision.transforms pipeline (RandomResizedCrop, RandomHorizontalFlip, Normalize with ImageNet stats). Returns train_loader, val_loader, num_classes. Uses ImageFolder.
       - `load_text_data(data_path, model_name, max_length=512, batch_size=16)`: Loads CSV/JSON with text+label columns, tokenizes with AutoTokenizer, returns train_loader, val_loader, num_labels.
       - This file uses module-level torch/torchvision/transformers imports. This is correct because prepare.py is a domain-specific file that gets COPIED into the experiment directory during scaffold -- it is never imported by mlforge core code. It runs in isolation during experiments where DL deps are installed.

    2. Update `pyproject.toml` to add BOTH optional dependency groups under `[project.optional-dependencies]`:
       - `dl = ["torch>=2.4", "torchvision>=0.19", "timm>=1.0", "transformers>=4.45", "datasets>=3.0"]`
       - `ft = ["peft>=0.14", "trl>=0.14", "bitsandbytes>=0.45", "evaluate>=0.4", "rouge-score>=0.1", "transformers>=4.45", "datasets>=3.0"]`
       NOTE: Both groups are added here to avoid parallel write conflicts with Plan 02 (which no longer touches pyproject.toml).

    3. Add tests for prepare.py functions to `tests/mlforge/test_dl_plugin.py`. Mock heavy imports (torch, torchvision, transformers) to avoid requiring GPU/DL deps for test runs.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python3 -m pytest tests/mlforge/test_dl_plugin.py -x -q --timeout=30</automated>
  </verify>
  <done>Frozen prepare.py has GPU detection, image loading, and text loading functions. pyproject.toml has both `dl` and `ft` optional dependency groups. All tests pass with mocked heavy deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DL train.py Jinja2 template with time budget + training features</name>
  <files>src/mlforge/templates/dl_train.py.j2, tests/mlforge/test_dl_plugin.py</files>
  <behavior>
    - Test: dl_train.py.j2 renders to valid Python with expected variables (csv_path/data_dir, metric, time_budget)
    - Test: Rendered template contains TIME_BUDGET_SEC variable
    - Test: Rendered template contains early stopping logic (patience counter, best_val_loss tracking)
    - Test: Rendered template contains gradient clipping (clip_grad_norm_)
    - Test: Rendered template contains LR scheduler (ReduceLROnPlateau)
    - Test: Rendered template contains wall-clock time check between epochs
    - Test: Rendered template handles both image_classification and text_classification tasks
    - Test: Rendered template for task=custom includes a minimal skeleton with training loop scaffold but no model creation (user fills in)
    - Test: Rendered template saves best model checkpoint (best_model.pt)
    - Test: Rendered template outputs JSON result with metric value for engine parsing
  </behavior>
  <action>
    1. Create `src/mlforge/templates/dl_train.py.j2` Jinja2 template:
       - Template variables: `{{ task }}` (image_classification, text_classification, or custom), `{{ data_dir }}` or `{{ data_path }}`, `{{ metric }}`, `{{ time_budget }}` (seconds), `{{ img_size }}` (default 224), `{{ batch_size }}` (default 32), `{{ model_name }}` (default "resnet50" for image, "distilbert-base-uncased" for text)
       - Rendered train.py structure:
         a. Imports (torch, timm or transformers depending on task)
         b. Configuration constants (TIME_BUDGET_SEC, PATIENCE=5, MAX_EPOCHS=100)
         c. Data loading via prepare.py functions
         d. Model creation (timm.create_model for image, AutoModelForSequenceClassification for text)
         e. For task=custom: include a `# TODO: Define your model here` placeholder with a minimal nn.Module skeleton, training loop with time budget/early stopping/gradient clipping already wired
         f. Optimizer (AdamW), scheduler (ReduceLROnPlateau)
         g. Training loop with:
            - Mixed precision via torch.amp.autocast + GradScaler
            - Gradient clipping: torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)
            - Early stopping: patience counter, track best_val_loss
            - Time budget: `if time.time() - start > TIME_BUDGET_SEC: break`
            - Best model save: `torch.save(model.state_dict(), "best_model.pt")`
         h. Final evaluation on validation set
         i. JSON output: `print(json.dumps({"metric_name": "{{ metric }}", "metric_value": val_metric}))`

    2. Update `tests/mlforge/test_dl_plugin.py` to add template rendering tests:
       - Render the template with image, text, AND custom task configs
       - Assert key patterns exist in rendered output (TIME_BUDGET_SEC, clip_grad_norm_, ReduceLROnPlateau, best_model.pt, json.dumps)
       - For custom task: verify the TODO placeholder and minimal skeleton are present
       - Verify rendered output is valid Python (compile with `compile()`)
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python3 -m pytest tests/mlforge/test_dl_plugin.py -x -q --timeout=30</automated>
  </verify>
  <done>dl_train.py.j2 renders valid Python train scripts for image classification, text classification, and custom architectures with time-budgeted training, early stopping, gradient clipping, LR scheduling, and JSON result output</done>
</task>

</tasks>

<verification>
- All existing tests still pass: `python3 -m pytest tests/mlforge/ -x -q --timeout=30`
- DL plugin conforms to DomainPlugin Protocol
- No torch import at mlforge package level (lazy imports only)
- Template renders valid Python for image, text, and custom tasks
- pyproject.toml has both `dl` and `ft` optional dependency groups
</verification>

<success_criteria>
- DeepLearningPlugin registers, scaffolds, validates config (including task=custom), and provides template context
- Frozen prepare.py has GPU detection, image loading, and text loading functions
- Mutable train.py template includes time budget, early stopping, gradient clipping, LR scheduling for all three task types
- All tests pass without requiring actual GPU or torch installation (mocked where needed)
- pyproject.toml has both `dl` and `ft` optional dependency groups
</success_criteria>

<output>
After completion, create `.planning/phases/05-domain-plugins-swarm/05-01-SUMMARY.md`
</output>
