---
phase: 05-domain-plugins-swarm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlforge/finetuning/__init__.py
  - src/mlforge/finetuning/prepare.py
  - src/mlforge/templates/ft_train.py.j2
  - tests/mlforge/test_ft_plugin.py
  - pyproject.toml
autonomous: true
requirements:
  - FT-01
  - FT-02
  - FT-03
  - FT-04
  - FT-05

must_haves:
  truths:
    - "FineTuningPlugin registers with plugin registry and passes isinstance(DomainPlugin) check"
    - "scaffold() creates frozen prepare.py with dataset formatting, chat template application, and VRAM info"
    - "scaffold() renders mutable train.py from ft_train.py.j2 with QLoRA config, SFTTrainer, and evaluation metrics"
    - "template_context() returns domain-specific fine-tuning rules for CLAUDE.md protocol"
    - "validate_config() rejects unknown FT metrics and missing model_name"
    - "All FT deps are lazy-imported -- base mlforge imports do not pull in peft/trl"
  artifacts:
    - path: "src/mlforge/finetuning/__init__.py"
      provides: "FineTuningPlugin class implementing DomainPlugin Protocol"
      exports: ["FineTuningPlugin"]
    - path: "src/mlforge/finetuning/prepare.py"
      provides: "Frozen dataset formatter with chat templates, VRAM reporting, train/eval splits"
      contains: "format_dataset"
    - path: "src/mlforge/templates/ft_train.py.j2"
      provides: "Jinja2 template for mutable train.py with QLoRA, SFTTrainer, perplexity/ROUGE eval"
      contains: "BitsAndBytesConfig"
    - path: "tests/mlforge/test_ft_plugin.py"
      provides: "Unit tests for FT-01 through FT-05"
      min_lines: 80
  key_links:
    - from: "src/mlforge/finetuning/__init__.py"
      to: "src/mlforge/plugins.py"
      via: "DomainPlugin Protocol conformance"
      pattern: "class FineTuningPlugin"
    - from: "src/mlforge/finetuning/__init__.py"
      to: "src/mlforge/templates/ft_train.py.j2"
      via: "Jinja2 template rendering in scaffold()"
      pattern: "env.get_template.*ft_train"
---

<objective>
Build the LLM fine-tuning plugin for mlforge, handling LoRA/QLoRA fine-tuning of open models via PEFT/TRL.

Purpose: Extends mlforge to LLM fine-tuning with VRAM management, quantization config, and evaluation metrics (perplexity, ROUGE).
Output: FineTuningPlugin class, frozen prepare.py, ft_train.py.j2 template, FT-specific CLAUDE.md rules, and comprehensive tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-domain-plugins-swarm/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/mlforge/plugins.py:
```python
@runtime_checkable
class DomainPlugin(Protocol):
    name: str
    frozen_files: list[str]
    def scaffold(self, target_dir: Path, config: Config) -> None: ...
    def template_context(self, config: Config) -> dict: ...
    def validate_config(self, config: Config) -> list[str]: ...

def register_plugin(plugin: DomainPlugin) -> None: ...
def get_plugin(name: str) -> DomainPlugin: ...
```

From src/mlforge/config.py:
```python
@dataclass
class Config:
    domain: str = "tabular"
    metric: str = "accuracy"
    direction: str = "maximize"
    plugin_settings: dict = field(default_factory=dict)
    frozen_files: list[str] = field(default_factory=lambda: ["prepare.py"])
    mutable_files: list[str] = field(default_factory=lambda: ["train.py"])
```

From src/mlforge/tabular/__init__.py (reference implementation):
```python
class TabularPlugin:
    name: str = "tabular"
    frozen_files: list[str] = ["prepare.py"]
    def scaffold(self, target_dir: Path, config: Config) -> None:
        prepare_source = Path(__file__).parent / "prepare.py"
        (target_dir / "prepare.py").write_text(prepare_source.read_text())
        env = get_template_env()
        template = env.get_template("tabular_train.py.j2")
        ...
    def template_context(self, config: Config) -> dict:
        return {"domain_rules": rules, "extra_sections": []}
    def validate_config(self, config: Config) -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FineTuningPlugin + frozen prepare.py + tests</name>
  <files>src/mlforge/finetuning/__init__.py, src/mlforge/finetuning/prepare.py, tests/mlforge/test_ft_plugin.py, pyproject.toml</files>
  <behavior>
    - Test: FineTuningPlugin satisfies isinstance(DomainPlugin) check
    - Test: plugin.name == "finetuning" and plugin.frozen_files == ["prepare.py"]
    - Test: scaffold() creates prepare.py and train.py in target directory
    - Test: scaffold() prepare.py contains format_dataset(), get_vram_info(), create_train_eval_split() functions
    - Test: validate_config() returns empty list for valid FT metrics (perplexity, rouge1, rougeL, loss)
    - Test: validate_config() returns error for unknown metric
    - Test: validate_config() warns if model_name not in plugin_settings
    - Test: template_context() returns dict with "domain_rules" list containing FT-specific rules
    - Test: template_context() rules mention LoRA, QLoRA, VRAM check, chat templates, no full fine-tune
    - Test: register_plugin(FineTuningPlugin()) succeeds and get_plugin("finetuning") returns it
    - Test: importing mlforge.finetuning does NOT import peft/trl/bitsandbytes (lazy import check)
  </behavior>
  <action>
    1. Add `src/mlforge/finetuning/__init__.py` with FineTuningPlugin class following TabularPlugin pattern:
       - `name = "finetuning"`, `frozen_files = ["prepare.py"]`
       - `scaffold()`: Copy frozen prepare.py; render mutable train.py from ft_train.py.j2. Template variables from config.plugin_settings: model_name (required), lora_r (default 16), lora_alpha (default 16), dataset_format (default "instruction"), max_length (default 512).
       - `template_context()`: Return domain_rules list with FT-specific rules:
         * Use LoRA/QLoRA adapters via peft -- NEVER do full fine-tuning
         * Check VRAM before loading model: if <16GB, use 4-bit quantization (QLoRA)
         * Always use tokenizer.apply_chat_template() for formatting -- never hand-construct prompts
         * Do not modify prepare.py -- frozen infrastructure
         * train.py is the ONLY mutable file
         * Save adapter weights only (not full model) via model.save_pretrained()
         * Use SFTTrainer from trl for training -- handles PEFT integration automatically
         * Evaluate with perplexity on held-out set after training
         * Monitor VRAM usage -- if >90% utilization, reduce batch size or max_length
         * Use gradient checkpointing to reduce memory: model.gradient_checkpointing_enable()
       - `validate_config()`: Check metric in {"perplexity", "rouge1", "rougeL", "rouge2", "loss"}, warn if plugin_settings missing "model_name".
       - ALL peft/trl/transformers/bitsandbytes imports MUST be inside methods, NOT at module level.

    2. Add `src/mlforge/finetuning/prepare.py` -- the frozen dataset formatting module:
       - `get_vram_info() -> dict`: Returns {"device": "cuda"/"cpu", "gpu_name": str|None, "vram_gb": float, "recommend_quantization": bool (True if vram_gb < 16)}.
       - `format_dataset(data_path, tokenizer_name, max_length=512, format="instruction") -> dict`: Loads JSON/JSONL/CSV dataset. Applies tokenizer.apply_chat_template() for chat format. Creates train/eval split (90/10). Returns {"train": train_dataset, "eval": eval_dataset, "num_labels": int}.
       - `create_train_eval_split(dataset, eval_fraction=0.1)`: Simple random split with seed for reproducibility.
       - Import transformers/datasets at module level (domain-specific file, only used during scaffold copy).

    3. Update `pyproject.toml` to add optional dependency group `ft = ["peft>=0.14", "trl>=0.14", "bitsandbytes>=0.45", "evaluate>=0.4", "rouge-score>=0.1", "transformers>=4.45", "datasets>=3.0"]`. NOTE: This plan touches pyproject.toml which Plan 01 also touches. If running in parallel, the second executor should read the file first and merge the extras group. Both plans add to `[project.optional-dependencies]` under different keys (dl vs ft) so they are additive.

    4. Write `tests/mlforge/test_ft_plugin.py` with all behaviors listed above. Mock peft/trl/transformers imports to avoid requiring heavy deps for tests.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python3 -m pytest tests/mlforge/test_ft_plugin.py -x -q --timeout=30</automated>
  </verify>
  <done>FineTuningPlugin passes all DomainPlugin Protocol checks, scaffold creates correct files, validate_config catches bad metrics and missing model_name, template_context returns FT rules, lazy imports verified, pyproject.toml has ft extras group</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FT train.py Jinja2 template with QLoRA + SFTTrainer + evaluation</name>
  <files>src/mlforge/templates/ft_train.py.j2, tests/mlforge/test_ft_plugin.py</files>
  <behavior>
    - Test: ft_train.py.j2 renders to valid Python with expected variables (model_name, lora_r, lora_alpha, metric)
    - Test: Rendered template contains BitsAndBytesConfig with load_in_4bit=True, bnb_4bit_quant_type="nf4"
    - Test: Rendered template contains LoraConfig with r and lora_alpha parameters
    - Test: Rendered template contains SFTTrainer setup
    - Test: Rendered template contains perplexity evaluation (math.exp of cross-entropy loss)
    - Test: Rendered template contains ROUGE evaluation option (evaluate.load("rouge"))
    - Test: Rendered template saves adapter weights via model.save_pretrained()
    - Test: Rendered template outputs JSON result with metric value for engine parsing
    - Test: Rendered template uses gradient_checkpointing_enable()
  </behavior>
  <action>
    1. Create `src/mlforge/templates/ft_train.py.j2` Jinja2 template:
       - Template variables: `{{ model_name }}`, `{{ lora_r }}` (default 16), `{{ lora_alpha }}` (default 16), `{{ metric }}`, `{{ max_length }}` (default 512), `{{ batch_size }}` (default 4), `{{ learning_rate }}` (default 2e-4), `{{ num_epochs }}` (default 3), `{{ dataset_format }}` (default "instruction")
       - Rendered train.py structure:
         a. Imports (torch, transformers, peft, trl, bitsandbytes)
         b. Configuration constants from template variables
         c. BitsAndBytesConfig: load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_compute_dtype=torch.bfloat16, bnb_4bit_use_double_quant=True
         d. Model loading: AutoModelForCausalLM.from_pretrained with quantization_config, device_map="auto"
         e. Tokenizer: AutoTokenizer.from_pretrained, set pad_token if missing
         f. LoraConfig: r={{ lora_r }}, lora_alpha={{ lora_alpha }}, target_modules="all-linear", lora_dropout=0.05, task_type=TaskType.CAUSAL_LM
         g. get_peft_model + gradient_checkpointing_enable()
         h. Dataset loading via prepare.py format_dataset()
         i. SFTTrainer with TrainingArguments (output_dir, per_device_train_batch_size, num_train_epochs, learning_rate, bf16=True, logging_steps=10)
         j. trainer.train()
         k. Evaluation:
            - Perplexity: run model on eval set, compute cross-entropy loss, math.exp(avg_loss)
            - ROUGE (conditional on metric): evaluate.load("rouge"), generate predictions, compute scores
         l. Save adapter: model.save_pretrained("best_adapter")
         m. JSON output: `print(json.dumps({"metric_name": "{{ metric }}", "metric_value": eval_metric}))`

    2. Update `tests/mlforge/test_ft_plugin.py` to add template rendering tests:
       - Render template with various configs (different model names, LoRA settings)
       - Assert key patterns: BitsAndBytesConfig, LoraConfig, SFTTrainer, save_pretrained, json.dumps
       - Verify rendered output compiles as valid Python (compile())
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python3 -m pytest tests/mlforge/test_ft_plugin.py -x -q --timeout=30</automated>
  </verify>
  <done>ft_train.py.j2 renders valid Python train scripts with QLoRA 4-bit quantization, LoRA adapters, SFTTrainer, perplexity/ROUGE evaluation, gradient checkpointing, and JSON result output</done>
</task>

</tasks>

<verification>
- All existing tests still pass: `python3 -m pytest tests/mlforge/ -x -q --timeout=30`
- FT plugin conforms to DomainPlugin Protocol
- No peft/trl import at mlforge package level (lazy imports only)
- Template renders valid Python with QLoRA config and evaluation
</verification>

<success_criteria>
- FineTuningPlugin registers, scaffolds, validates config, and provides template context
- Frozen prepare.py has VRAM detection, dataset formatting with chat templates, and train/eval splits
- Mutable train.py template includes QLoRA, LoRA adapters, SFTTrainer, perplexity/ROUGE eval
- All tests pass without requiring actual GPU or peft/trl installation (mocked)
- pyproject.toml has `ft` optional dependency group
</success_criteria>

<output>
After completion, create `.planning/phases/05-domain-plugins-swarm/05-02-SUMMARY.md`
</output>
