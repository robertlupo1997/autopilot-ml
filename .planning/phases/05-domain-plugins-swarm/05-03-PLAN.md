---
phase: 05-domain-plugins-swarm
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlforge/swarm/__init__.py
  - src/mlforge/swarm/scoreboard.py
  - src/mlforge/swarm/verifier.py
  - tests/mlforge/test_swarm.py
  - tests/mlforge/test_scoreboard.py
autonomous: true
requirements:
  - SWARM-01
  - SWARM-02
  - SWARM-03
  - SWARM-04

must_haves:
  truths:
    - "SwarmManager creates git worktrees for N parallel agents and spawns claude -p subprocesses"
    - "SwarmScoreboard uses fcntl.LOCK_EX for atomic read-check-write-append cycles"
    - "Budget inheritance splits parent budget across N children with no recursive swarm spawning"
    - "Verification agent re-runs holdout evaluation and compares claimed vs actual metric"
    - "SwarmManager.teardown() cleans up all worktrees even after crashes (finally block)"
  artifacts:
    - path: "src/mlforge/swarm/__init__.py"
      provides: "SwarmManager class for parallel agent orchestration"
      exports: ["SwarmManager"]
    - path: "src/mlforge/swarm/scoreboard.py"
      provides: "File-locked TSV scoreboard for cross-agent coordination"
      exports: ["SwarmScoreboard"]
    - path: "src/mlforge/swarm/verifier.py"
      provides: "Verification agent that re-runs holdout evaluation"
      exports: ["verify_best_result"]
    - path: "tests/mlforge/test_swarm.py"
      provides: "Unit tests for SWARM-01, SWARM-03, SWARM-04"
      min_lines: 80
    - path: "tests/mlforge/test_scoreboard.py"
      provides: "Unit tests for SWARM-02 (file-locked scoreboard)"
      min_lines: 40
  key_links:
    - from: "src/mlforge/swarm/__init__.py"
      to: "src/mlforge/swarm/scoreboard.py"
      via: "SwarmManager uses SwarmScoreboard for coordination"
      pattern: "SwarmScoreboard"
    - from: "src/mlforge/swarm/__init__.py"
      to: "src/mlforge/config.py"
      via: "Budget inheritance creates child Config objects"
      pattern: "Config\\("
    - from: "src/mlforge/swarm/__init__.py"
      to: "src/mlforge/git_ops.py"
      via: "GitManager for worktree add/remove"
      pattern: "git\\.worktree"
---

<objective>
Build swarm mode for mlforge -- parallel agent exploration in git worktrees with file-locked scoreboard coordination, budget inheritance, and metric verification.

Purpose: Enables parallel model exploration (3-5 agents trying different approaches simultaneously) for faster convergence on complex ML problems.
Output: SwarmManager, SwarmScoreboard, verification agent, and comprehensive tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-domain-plugins-swarm/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/mlforge/config.py:
```python
@dataclass
class Config:
    domain: str = "tabular"
    metric: str = "accuracy"
    direction: str = "maximize"
    budget_minutes: int = 60
    budget_experiments: int = 50
    budget_usd: float = 5.0
    per_experiment_timeout_sec: int = 300
    per_experiment_budget_usd: float = 1.0
    max_turns_per_experiment: int = 30
    model: str | None = None
    frozen_files: list[str] = field(default_factory=lambda: ["prepare.py"])
    mutable_files: list[str] = field(default_factory=lambda: ["train.py"])
    plugin_settings: dict = field(default_factory=dict)
```

From src/mlforge/git_ops.py (relevant methods):
```python
class GitManager:
    def __init__(self, repo_path: Path): ...
    # GitPython repo available as self.repo
    # Has branch creation, commit, reset, tag operations
```

Old reference implementation (src/automl/swarm.py):
- SwarmManager creates worktrees under .swarm/agent-N/
- Uses subprocess.Popen for claude -p per worktree
- signal.SIGINT handler for graceful shutdown
- teardown() removes all worktrees in finally block

Old reference (src/automl/swarm_scoreboard.py):
- TSV format: agent\tcommit\tmetric_value\telapsed_sec\tstatus\tdescription\ttimestamp
- fcntl.LOCK_EX for atomic write, lockless reads for display
- publish_result() holds lock for full read-check-write cycle
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SwarmScoreboard with file-locked coordination + tests</name>
  <files>src/mlforge/swarm/__init__.py, src/mlforge/swarm/scoreboard.py, tests/mlforge/test_scoreboard.py</files>
  <behavior>
    - Test: SwarmScoreboard creates TSV file with header on first publish
    - Test: publish_result() appends a row and returns True if new global best (maximize direction)
    - Test: publish_result() returns False if not a new global best
    - Test: publish_result() works correctly with minimize direction
    - Test: read_best() returns (best_score, best_agent) tuple or (None, None) if empty
    - Test: read_all() returns list of all result dicts
    - Test: Concurrent writes from multiple threads do not corrupt scoreboard (threading test)
    - Test: Scoreboard file survives crash (append-only, no rewrite)
    - Test: Lock file is created alongside scoreboard
  </behavior>
  <action>
    1. Create `src/mlforge/swarm/__init__.py` with just the package docstring and `__all__` for now. Will be expanded in Task 2.

    2. Create `src/mlforge/swarm/scoreboard.py` with SwarmScoreboard class:
       - `__init__(self, scoreboard_path: Path, lock_path: Path | None = None, direction: str = "maximize")`:
         * scoreboard_path: Path to .swarm/scoreboard.tsv
         * lock_path: defaults to scoreboard_path.with_suffix(".lock")
         * direction: "maximize" or "minimize" for comparison
       - TSV HEADER: "agent\tcommit\tmetric_value\telapsed_sec\tstatus\tdescription\ttimestamp\n"
       - `publish_result(self, agent: str, commit: str, metric_value: float, elapsed_sec: float, status: str, description: str) -> bool`:
         * Acquire fcntl.LOCK_EX on lock file
         * Create scoreboard with header if not exists
         * Read current best while holding lock
         * Append new row with timestamp (datetime.utcnow().isoformat())
         * Compare: return True if new global best
         * Release lock in finally block
         * For maximize: new > current_best. For minimize: new < current_best.
       - `read_best(self) -> tuple[float | None, str | None]`:
         * Lockless read (safe for display)
         * Parse all rows, find best by direction
         * Return (best_metric, best_agent) or (None, None) if empty
       - `read_all(self) -> list[dict]`:
         * Lockless read, parse all rows into list of dicts
       - Use `fcntl.flock(fd, fcntl.LOCK_EX)` and `fcntl.flock(fd, fcntl.LOCK_UN)` pattern.
       - Handle platform: if fcntl not available (Windows), raise RuntimeError with clear message "Swarm mode requires Unix (fcntl). Use WSL on Windows."

    3. Write `tests/mlforge/test_scoreboard.py`:
       - Use tmp_path fixture for isolated scoreboard files
       - Test all behaviors above
       - For threading test: 5 threads each publish 10 results concurrently, verify total row count = 50 and no corruption
       - Mock fcntl if not available on test platform (though this project targets Linux/WSL)
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python3 -m pytest tests/mlforge/test_scoreboard.py -x -q --timeout=30</automated>
  </verify>
  <done>SwarmScoreboard creates TSV, publishes with LOCK_EX, reads locklessly, handles concurrent writes without corruption, supports both maximize/minimize directions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SwarmManager + budget inheritance + verifier + tests</name>
  <files>src/mlforge/swarm/__init__.py, src/mlforge/swarm/verifier.py, tests/mlforge/test_swarm.py</files>
  <behavior>
    - Test: SwarmManager.__init__ accepts config, n_agents, experiment_dir
    - Test: create_child_configs() splits budget_usd, budget_minutes, budget_experiments evenly across N agents
    - Test: Child configs have swarm disabled (no recursive spawning) -- no swarm_enabled or n_agents set
    - Test: setup() creates .swarm/ directory and N worktree directories
    - Test: setup() calls git worktree add for each agent (mocked GitPython)
    - Test: teardown() removes all worktrees and prunes (mocked GitPython)
    - Test: teardown() works even if some worktrees already removed (crash recovery)
    - Test: _build_agent_command() produces correct claude -p command with child config
    - Test: verify_best_result() re-runs evaluation script and returns claimed vs actual comparison
    - Test: verify_best_result() returns match=True when abs(claimed - actual) < tolerance
    - Test: verify_best_result() returns match=False when metrics diverge
    - Test: Budget math: 3 agents from $6 budget = $2 each; 15 experiments / 3 = 5 each
  </behavior>
  <action>
    1. Update `src/mlforge/swarm/__init__.py` with SwarmManager class:
       - `__init__(self, config: Config, experiment_dir: Path, n_agents: int = 3)`:
         * Stores config, experiment_dir, n_agents
         * Creates swarm_dir = experiment_dir / ".swarm"
         * Creates scoreboard: SwarmScoreboard(swarm_dir / "scoreboard.tsv", direction=config.direction)
       - `create_child_configs(self) -> list[Config]`:
         * Split budget_usd / n_agents, budget_minutes / n_agents, budget_experiments // n_agents
         * Each child Config inherits domain, metric, direction, per_experiment settings, plugin_settings
         * Children get NO swarm capability (they are leaf agents)
       - `setup(self) -> list[Path]`:
         * Create swarm_dir
         * For each agent 0..n_agents-1: git worktree add .swarm/agent-{i} (from current HEAD)
         * Write child config TOML in each worktree
         * Return list of worktree paths
       - `run(self) -> dict`:
         * For each agent: subprocess.Popen(_build_agent_command(i))
         * Wait for all agents to complete
         * Read scoreboard for results
         * Return {"agents": n_agents, "best_score": X, "best_agent": Y, "results": all_results}
       - `_build_agent_command(self, agent_index: int) -> list[str]`:
         * Returns ["claude", "-p", prompt, "--cwd", str(worktree_path)]
         * Prompt instructs agent to run experiments per CLAUDE.md, publish results to scoreboard
       - `teardown(self)`:
         * For each worktree: git worktree remove --force
         * git worktree prune as fallback
         * Remove swarm_dir if empty
         * MUST be called in finally block by caller
       - Signal handling: register SIGINT handler that sets _stop_requested flag

    2. Create `src/mlforge/swarm/verifier.py` with verify_best_result():
       - `verify_best_result(experiment_dir: Path, scoreboard: SwarmScoreboard, eval_script: str = "python train.py --eval-only") -> dict`:
         * Read best result from scoreboard (agent, commit, claimed_metric)
         * Checkout best agent's commit in a temporary worktree
         * Run eval_script via subprocess (read-only evaluation, no training)
         * Parse JSON output for actual metric value
         * Compare claimed vs actual: match = abs(claimed - actual) < 0.001 (tolerance)
         * Return {"claimed_metric": float, "verified_metric": float, "match": bool, "agent": str, "commit": str}
         * Cleanup temporary worktree in finally block

    3. Write `tests/mlforge/test_swarm.py`:
       - Mock GitPython (repo.git.worktree) for worktree operations
       - Mock subprocess.Popen for agent spawning
       - Test all behaviors above
       - Budget inheritance math: parametrize with various n_agents and budget values
       - Verifier: mock subprocess output with JSON metric, test match/mismatch cases
       - Teardown resilience: mock some worktrees already removed, verify no crash
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python3 -m pytest tests/mlforge/test_swarm.py -x -q --timeout=30</automated>
  </verify>
  <done>SwarmManager creates worktrees, spawns agents, splits budgets correctly, prevents recursive spawning. Verifier re-evaluates best model and detects metric mismatches. Teardown handles crash recovery gracefully.</done>
</task>

</tasks>

<verification>
- All existing tests still pass: `python3 -m pytest tests/mlforge/ -x -q --timeout=30`
- Scoreboard handles concurrent writes without corruption
- Budget inheritance math is correct (no off-by-one, no spawn explosion)
- Verifier catches metric discrepancies
- Teardown cleans up even after partial failures
</verification>

<success_criteria>
- SwarmManager orchestrates N parallel agents in git worktrees
- SwarmScoreboard uses fcntl.LOCK_EX for atomic coordination
- Budget splits evenly with no recursive spawning allowed
- Verification agent re-runs evaluation and reports claimed vs actual metric
- All tests pass with mocked git/subprocess operations
</success_criteria>

<output>
After completion, create `.planning/phases/05-domain-plugins-swarm/05-03-SUMMARY.md`
</output>
