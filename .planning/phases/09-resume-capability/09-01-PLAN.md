---
phase: 09-resume-capability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automl/checkpoint.py
  - src/automl/scaffold.py
  - tests/test_checkpoint.py
  - tests/test_scaffold.py
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-05

must_haves:
  truths:
    - "save_checkpoint() writes valid JSON to checkpoint.json atomically (write-then-rename)"
    - "load_checkpoint() returns dict when file exists, None when absent or corrupt"
    - "load_loop_state() reconstructs LoopState from checkpoint, filtering unknown fields"
    - "checkpoint_exists() returns True/False based on file presence"
    - "LoopState round-trips through save/load without data loss"
    - "Scaffolded .gitignore includes checkpoint.json and checkpoint.json.tmp"
  artifacts:
    - path: "src/automl/checkpoint.py"
      provides: "Checkpoint persistence module"
      exports: ["save_checkpoint", "load_checkpoint", "load_loop_state", "checkpoint_exists", "CHECKPOINT_FILE", "SCHEMA_VERSION"]
    - path: "tests/test_checkpoint.py"
      provides: "Unit tests for checkpoint module"
      min_lines: 80
  key_links:
    - from: "src/automl/checkpoint.py"
      to: "src/automl/loop_helpers.py"
      via: "import LoopState for deserialization"
      pattern: "from automl\\.loop_helpers import LoopState"
    - from: "src/automl/scaffold.py"
      to: "checkpoint.json"
      via: "_gitignore_content includes checkpoint files"
      pattern: "checkpoint\\.json"
---

<objective>
Create the checkpoint.py persistence module and update scaffold.py gitignore.

Purpose: Provides the data layer for session resume -- saving and loading LoopState + metadata to/from checkpoint.json with atomic writes. Also ensures checkpoint files are git-ignored in scaffolded experiments.

Output: src/automl/checkpoint.py, updated scaffold.py _gitignore_content, comprehensive tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-resume-capability/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/automl/loop_helpers.py:
```python
@dataclass
class LoopState:
    best_score: float | None = None
    best_commit: str | None = None
    consecutive_reverts: int = 0
    consecutive_crashes: int = 0
    last_crash_error: str | None = None
    total_experiments: int = 0
    total_keeps: int = 0
    total_reverts: int = 0
    total_crashes: int = 0
    strategy_categories_tried: list[str] = field(default_factory=list)
    stagnation_threshold: int = 5
    crash_threshold: int = 3
```

From src/automl/scaffold.py:
```python
def _gitignore_content() -> str:
    """Return .gitignore content for experiment directories."""
    return textwrap.dedent("""\
        results.tsv
        run.log
        __pycache__/
        *.pyc
        .venv/
        .claude/settings.local.json
    """)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create checkpoint.py module with tests</name>
  <files>src/automl/checkpoint.py, tests/test_checkpoint.py</files>
  <behavior>
    - save_checkpoint(LoopState(...), "iteration", 5, tmp_dir) creates checkpoint.json with all LoopState fields + loop_phase + iteration + schema_version=1
    - save_checkpoint uses write-then-rename: writes to .json.tmp first, then renames to .json
    - load_checkpoint(tmp_dir) returns dict with all fields when file exists
    - load_checkpoint returns None when file absent
    - load_checkpoint returns None when file contains invalid JSON (corrupt)
    - load_loop_state(tmp_dir) returns LoopState with correct field values
    - load_loop_state filters out non-LoopState fields (loop_phase, iteration, schema_version)
    - load_loop_state returns None when no checkpoint
    - checkpoint_exists returns True when file present, False when absent
    - Round-trip: LoopState -> save -> load_loop_state produces equivalent LoopState
    - Round-trip preserves list fields (strategy_categories_tried)
    - Round-trip preserves None fields (best_score=None, best_commit=None)
  </behavior>
  <action>
    Write tests/test_checkpoint.py FIRST with all behaviors above as failing tests. Use tmp_path fixture for isolation. Test classes: TestSaveCheckpoint, TestLoadCheckpoint, TestLoadLoopState, TestCheckpointExists, TestRoundTrip, TestAtomicWrite.

    Then create src/automl/checkpoint.py following the exact implementation from 09-RESEARCH.md Pattern 1 + Pattern 2:
    - CHECKPOINT_FILE = "checkpoint.json", SCHEMA_VERSION = 1
    - save_checkpoint(loop_state, loop_phase, iteration, path=".") -- uses dataclasses.asdict(), writes .json.tmp then renames
    - load_checkpoint(path=".") -- returns dict or None, catches JSONDecodeError and OSError
    - load_loop_state(path=".") -- filters to known LoopState fields using dataclasses.fields(), returns LoopState(**filtered) or None
    - checkpoint_exists(path=".") -- simple Path.exists() check

    All stdlib -- json, dataclasses (asdict, fields), pathlib. No external deps.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_checkpoint.py -x -q</automated>
  </verify>
  <done>All checkpoint module tests pass. save/load/exists functions work correctly with atomic writes. LoopState round-trips without data loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update scaffold.py gitignore to include checkpoint files</name>
  <files>src/automl/scaffold.py, tests/test_scaffold.py</files>
  <behavior>
    - _gitignore_content() output contains "checkpoint.json"
    - _gitignore_content() output contains "checkpoint.json.tmp"
    - Existing gitignore entries (results.tsv, run.log, __pycache__/, etc.) still present
  </behavior>
  <action>
    Add test to tests/test_scaffold.py FIRST: a test that calls _gitignore_content() and asserts "checkpoint.json" and "checkpoint.json.tmp" are both in the returned string. Also assert existing entries (results.tsv, run.log) still present.

    Then update _gitignore_content() in src/automl/scaffold.py to add two lines after "run.log":
    ```
    checkpoint.json
    checkpoint.json.tmp
    ```

    This is a 2-line addition to an existing function. Do NOT change any other scaffold.py code.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_scaffold.py -x -q</automated>
  </verify>
  <done>_gitignore_content() includes checkpoint.json and checkpoint.json.tmp. All existing scaffold tests still pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/tlupo/AutoML && uv run pytest tests/test_checkpoint.py tests/test_scaffold.py -x -q
```
All checkpoint and scaffold tests pass. No regressions.
</verification>

<success_criteria>
- checkpoint.py exports save_checkpoint, load_checkpoint, load_loop_state, checkpoint_exists
- Atomic write-then-rename pattern used for save
- Corrupt/missing files handled gracefully (return None)
- LoopState round-trips without data loss
- scaffold.py gitignore includes checkpoint.json and checkpoint.json.tmp
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09-resume-capability/09-01-SUMMARY.md`
</output>
