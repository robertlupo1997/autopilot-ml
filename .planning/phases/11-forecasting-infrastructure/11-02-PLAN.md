---
phase: 11-forecasting-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automl/prepare.py
  - tests/test_prepare.py
  - tests/conftest.py
autonomous: true
requirements:
  - TVAL-01
  - TVAL-02
  - TVAL-03

must_haves:
  truths:
    - "load_data with date_col returns DataFrame with sorted DatetimeIndex"
    - "temporal_split returns time-ordered train/holdout with no shuffle"
    - "Existing prepare.py functions remain unchanged and all existing tests pass"
  artifacts:
    - path: "src/automl/prepare.py"
      provides: "Extended load_data with date_col parameter, new temporal_split function"
      contains: "temporal_split"
    - path: "tests/test_prepare.py"
      provides: "Tests for load_data date_col and temporal_split"
      contains: "TestLoadDataForecast"
  key_links:
    - from: "src/automl/prepare.py::load_data"
      to: "pd.read_csv with parse_dates"
      via: "conditional date_col handling"
      pattern: "parse_dates"
    - from: "src/automl/prepare.py::temporal_split"
      to: "DataFrame.iloc slicing"
      via: "time-ordered split without shuffle"
      pattern: "iloc\\["
---

<objective>
Extend `prepare.py` with date-column support in `load_data` and add a new `temporal_split` function for time-ordered train/holdout splitting. All changes are backwards-compatible -- existing behavior is preserved when `date_col` is not provided.

Purpose: The forecasting pipeline needs time-ordered data loading (sorted DatetimeIndex) and time-ordered splitting (no shuffle). These extensions to the frozen `prepare.py` module provide that foundation while preserving all existing PIPE-01 through PIPE-07 functionality.

Output: Extended `src/automl/prepare.py`, extended `tests/test_prepare.py`, updated `tests/conftest.py` (shared fixture for forecasting CSVs).
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-forecasting-infrastructure/11-RESEARCH.md

<interfaces>
<!-- Current load_data signature to extend -->
From src/automl/prepare.py:
```python
def load_data(
    csv_path: str | Path, target_column: str
) -> tuple[pd.DataFrame, pd.Series, str]:
    """Read a CSV and return features, target, and inferred task type."""
    df = pd.read_csv(csv_path)
    y = df[target_column]
    X = df.drop(columns=[target_column])
    # ... task inference ...
    return X, y, task
```

<!-- Existing conftest.py fixtures -->
From tests/conftest.py:
```python
@pytest.fixture
def sample_classification_csv(tmp_path: Path) -> Path: ...
@pytest.fixture
def sample_regression_csv(tmp_path: Path) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend load_data with date_col and add temporal_split</name>
  <files>src/automl/prepare.py, tests/test_prepare.py, tests/conftest.py</files>
  <behavior>
    - load_data(csv, target, date_col="date") parses date column as datetime, sets as index, sorts ascending, returns X with DatetimeIndex
    - load_data(csv, target) without date_col behaves identically to current implementation (backwards-compatible)
    - load_data with date_col infers task as "regression" (time-series targets are continuous)
    - temporal_split(X, y, holdout_fraction=0.15) returns (X_train, X_holdout, y_train, y_holdout) where last 15% of rows are holdout
    - temporal_split invariant: X_train.index[-1] < X_holdout.index[0] (no temporal leakage)
    - temporal_split does NOT shuffle
  </behavior>
  <action>
    1. Add `sample_forecast_csv` fixture to `tests/conftest.py` — 40-row CSV with columns: `date` (quarterly dates Q1 2015 to Q4 2024), `feature1` (numeric), `feature2` (numeric), `revenue` (target, dollar values with trend). Write to tmp_path and return path.

    2. Add RED tests to `tests/test_prepare.py`:
       - `TestLoadDataForecast`:
         - `test_date_col_sets_datetime_index`: load_data with date_col returns X with DatetimeIndex
         - `test_date_col_sorted_ascending`: X.index is monotonically increasing
         - `test_date_col_drops_date_from_features`: date column not in X.columns
         - `test_date_col_infers_regression`: task == "regression" for numeric revenue target
         - `test_no_date_col_unchanged`: Without date_col, behavior matches existing (RangeIndex, same shape)
       - `TestTemporalSplit`:
         - `test_returns_four_parts`: Returns (X_train, X_holdout, y_train, y_holdout)
         - `test_holdout_is_last_fraction`: With 40 rows and 0.15, holdout is last 6 rows, train is first 34
         - `test_no_shuffle_invariant`: X_train.index[-1] < X_holdout.index[0] when DatetimeIndex
         - `test_no_data_loss`: len(train) + len(holdout) == len(original)

    3. Extend `src/automl/prepare.py` to make tests GREEN:
       - Update `load_data` signature: add `date_col: str | None = None` parameter
       - When `date_col` is provided:
         - `df = pd.read_csv(csv_path, parse_dates=[date_col])`
         - `df = df.set_index(date_col).sort_index()`
         - Task inference still runs on y dtype, but continuous revenue values will naturally be "regression"
       - When `date_col` is None: existing behavior unchanged (no parse_dates, no index change)
       - Add `temporal_split(X, y, holdout_fraction=0.15)` function:
         - `import math`
         - `split_idx = math.floor(len(X) * (1 - holdout_fraction))`
         - Return `(X.iloc[:split_idx], X.iloc[split_idx:], y.iloc[:split_idx], y.iloc[split_idx:])`
         - Docstring notes the no-shuffle invariant and that data must be pre-sorted
       - Add imports: `import math` at top of file
       - Add `temporal_split` to module-level exports if `__all__` exists

    4. Run `uv run pytest tests/test_prepare.py -x -q` — all tests pass (new + existing)
    5. Run `uv run pytest -x -q` — full suite green

    Key constraint from research: `date_col` defaults to None for backwards compatibility (Pitfall 3). All existing tests MUST remain green. Do NOT change the return type or existing behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/test_prepare.py -x -q</automated>
  </verify>
  <done>
    - load_data accepts optional date_col parameter; with date_col returns sorted DatetimeIndex DataFrame
    - temporal_split returns time-ordered train/holdout split without shuffle
    - All existing test_prepare.py tests still pass (zero regressions)
    - Full test suite green
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_prepare.py -x -v` — all prepare tests pass including new forecast tests
- `uv run pytest -x -q` — full suite green
- `python -c "from automl.prepare import load_data, temporal_split; print('New exports available')"` — importable
</verification>

<success_criteria>
- load_data with date_col returns sorted DatetimeIndex DataFrame
- load_data without date_col behaves identically to v1.0
- temporal_split returns time-ordered split with no shuffle
- All existing tests pass (backwards compatibility verified)
</success_criteria>

<output>
After completion, create `.planning/phases/11-forecasting-infrastructure/11-02-SUMMARY.md`
</output>
