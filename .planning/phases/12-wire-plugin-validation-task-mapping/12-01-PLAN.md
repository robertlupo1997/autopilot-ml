---
phase: 12-wire-plugin-validation-task-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlforge/scaffold.py
  - tests/mlforge/test_scaffold.py
autonomous: true
requirements:
  - FT-04
  - DL-03
  - UX-01
  - TABL-01
  - DL-01
  - FT-01

must_haves:
  truths:
    - "Invalid configs are rejected before scaffolding with clear error messages"
    - "Profiler task types map to DL-specific types so simple mode works for all domains"
    - "FineTuningPlugin rejects missing model_name with actionable error"
    - "DL-native task types (image_classification, text_classification) pass through unmapped"
    - "Tabular task types pass through unchanged"
  artifacts:
    - path: "src/mlforge/scaffold.py"
      provides: "validate_config() call + _map_task_for_domain() function"
      contains: "validate_config"
    - path: "tests/mlforge/test_scaffold.py"
      provides: "Tests for validation wiring and task mapping"
      contains: "TestTaskTypeMapping"
  key_links:
    - from: "src/mlforge/scaffold.py"
      to: "plugin.validate_config()"
      via: "call before plugin.scaffold()"
      pattern: "validate_config"
    - from: "src/mlforge/scaffold.py"
      to: "config.plugin_settings"
      via: "_map_task_for_domain mutates task key"
      pattern: "_map_task_for_domain"
---

<objective>
Wire plugin validate_config() into scaffold_experiment() and add task type mapping so profiler outputs work for all domains.

Purpose: Close GAP-4 (validate_config dead code) and GAP-5 (DL/FT task type mismatch) from the v1.0 milestone audit. Currently validate_config() is defined on all 3 plugins but never called, and the profiler outputs `classification`/`regression` but DL expects `image_classification`/`text_classification`.

Output: Updated scaffold.py with validation gate + task mapping, new test classes covering both features.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-wire-plugin-validation-task-mapping/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/mlforge/scaffold.py:
```python
def scaffold_experiment(config: Config, dataset_path: Path, target_dir: Path, run_id: str) -> Path
def _ensure_plugin_registered(domain: str) -> None
```

From src/mlforge/plugins.py:
```python
def validate_config(self, config: Config) -> list[str]:  # Protocol method on DomainPlugin
def get_plugin(domain: str) -> DomainPlugin
```

From src/mlforge/config.py:
```python
@dataclass
class Config:
    domain: str = "tabular"
    metric: str = "r2"
    plugin_settings: dict = field(default_factory=dict)
    # ... other fields
```

DL valid tasks: {"image_classification", "text_classification", "custom"}
DL valid metrics: {"accuracy", "f1", "precision", "recall", "auc"}
FT valid metrics: {"perplexity", "rouge1", "rouge2", "rougeL"}
Tabular valid metrics: {"r2", "accuracy", "f1_weighted", "neg_mean_absolute_error", "neg_root_mean_squared_error", "roc_auc"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _map_task_for_domain() and validate_config() gate to scaffold_experiment()</name>
  <files>src/mlforge/scaffold.py, tests/mlforge/test_scaffold.py</files>
  <behavior>
    - Test: scaffold rejects tabular config with invalid metric (e.g., "nonexistent_metric") -- raises ValueError matching "Invalid config.*tabular"
    - Test: scaffold rejects finetuning config without model_name -- raises ValueError matching "model_name"
    - Test: scaffold rejects DL config with invalid task type (e.g., "classification" without mapping would fail, but mapping runs first -- so test with truly invalid like "invalid_task") -- raises ValueError
    - Test: DL config with profiler task "classification" gets mapped to "image_classification" before validation
    - Test: DL config with profiler task "regression" gets mapped to "custom" before validation
    - Test: DL config with native task "text_classification" is NOT remapped (passes through)
    - Test: Tabular config with task "classification" is NOT mapped (passes through)
    - Test: FT config ignores task mapping (no mapping defined for finetuning domain)
    - Test: Valid tabular config still scaffolds successfully (no regression)
  </behavior>
  <action>
1. In `tests/mlforge/test_scaffold.py`, add two new test classes:

**`TestScaffoldValidation`** (extend the existing class that only has `test_nonexistent_dataset_raises`):
- `test_rejects_invalid_tabular_metric`: Config(domain="tabular", metric="nonexistent") should raise ValueError matching "Invalid config.*tabular"
- `test_rejects_ft_without_model_name`: Config(domain="finetuning", metric="perplexity") with no model_name in plugin_settings should raise ValueError matching "model_name"
- `test_rejects_dl_invalid_task`: Config(domain="deeplearning", metric="accuracy") with plugin_settings={"task": "invalid_task"} should raise ValueError matching "Unknown deeplearning task"
- `test_valid_config_scaffolds_ok`: Config(domain="tabular") should scaffold without error (regression check)

**`TestTaskTypeMapping`** (new class):
- `test_dl_classification_mapped_to_image_classification`: Config(domain="deeplearning", metric="accuracy") with plugin_settings={"task": "classification"} -- after scaffold, check config.plugin_settings["task"] == "image_classification" (or check train.py contains image_classification content)
- `test_dl_regression_mapped_to_custom`: Config(domain="deeplearning", metric="accuracy") with plugin_settings={"task": "regression"} -- mapped to "custom"
- `test_dl_native_task_not_remapped`: plugin_settings={"task": "text_classification"} stays "text_classification"
- `test_tabular_task_passthrough`: Config(domain="tabular") with plugin_settings={"task": "classification"} -- task stays "classification"
- `test_ft_task_no_mapping`: Config(domain="finetuning", metric="perplexity") with plugin_settings={"task": "classification", "model_name": "test/model"} -- task stays "classification"

Note: Both test classes need `setup_method` that clears `_registry` (same pattern as TestPluginRegistrationDispatch).

2. In `src/mlforge/scaffold.py`, add:

**`_TASK_TYPE_MAP`** dict at module level (after `_REGISTRATION_FUNCTIONS`):
```python
_TASK_TYPE_MAP: dict[str, dict[str, str]] = {
    "deeplearning": {
        "classification": "image_classification",
        "regression": "custom",
    },
}
```

**`_map_task_for_domain(config: Config) -> None`** function:
- Gets domain mapping from `_TASK_TYPE_MAP`. If None, return (no mapping for this domain).
- Gets current task from `config.plugin_settings.get("task")`.
- If current task exists AND is in the domain map, replace it: `config.plugin_settings["task"] = task_map[current_task]`.
- Mutates config in-place (established pattern -- CLI already does this at line 164 of scaffold.py).

**In `scaffold_experiment()`**, between step 2 (get plugin) and step 3 (plugin.scaffold), insert:
```python
# 2b. Map profiler task types to domain-specific types
_map_task_for_domain(config)

# 2c. Validate config before scaffolding
errors = plugin.validate_config(config)
if errors:
    raise ValueError(
        f"Invalid config for {config.domain} plugin:\n"
        + "\n".join(f"  - {e}" for e in errors)
    )
```

Run tests RED first, then implement, then GREEN.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/mlforge/test_scaffold.py -x -q</automated>
  </verify>
  <done>
    - validate_config() called in scaffold_experiment() before plugin.scaffold()
    - Invalid tabular metric, missing FT model_name, and invalid DL task all raise ValueError
    - _map_task_for_domain() translates classification->image_classification and regression->custom for DL
    - DL-native types and tabular/FT types pass through unmapped
    - All existing scaffold tests still pass (no regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite regression check</name>
  <files></files>
  <action>
Run the full test suite to confirm no regressions from scaffold.py changes. The validate_config gate could theoretically break existing tests if any test uses an invalid metric without realizing it. If any tests fail, fix them by ensuring their Config objects use valid metrics for their domain.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Full test suite passes with 0 failures. No regressions from validate_config wiring or task type mapping.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/mlforge/test_scaffold.py::TestScaffoldValidation -x -q` -- all validation gate tests pass
2. `python -m pytest tests/mlforge/test_scaffold.py::TestTaskTypeMapping -x -q` -- all task mapping tests pass
3. `python -m pytest tests/ -x -q` -- full suite green
4. `grep -n "validate_config" src/mlforge/scaffold.py` -- confirms validate_config is called
5. `grep -n "_map_task_for_domain" src/mlforge/scaffold.py` -- confirms mapping function exists and is called
</verification>

<success_criteria>
- validate_config() is called in scaffold_experiment() before plugin.scaffold()
- _map_task_for_domain() maps profiler types to DL-specific types
- Invalid configs raise ValueError with clear domain-specific messages
- All 3 plugins' validation logic is exercised through scaffold (tabular metric, DL task, FT model_name)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-wire-plugin-validation-task-mapping/12-01-SUMMARY.md`
</output>
