# Deferred Items - Phase 12

## Pre-existing Issues (Out of Scope)

1. **tests/test_cli.py::test_cli_valid_args** - Fails with "string dtypes are not allowed, use 'object' instead". This is in the old `automl` package (not mlforge). Pre-existing failure unrelated to phase 12 changes.
