---
phase: 14-e2e-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/quarterly_revenue.csv
  - scripts/run-forecast-validation-test.sh
  - tests/test_phase14_validation.py
  - .planning/phases/14-e2e-validation/FINDINGS.md
autonomous: false
requirements: [EVAL-01, EVAL-02]

must_haves:
  truths:
    - "quarterly_revenue.csv has 40 rows with quarter and revenue columns"
    - "run-forecast-validation-test.sh scaffolds a forecasting experiment with --date-column quarter"
    - "run-forecast-validation-test.sh runs claude -p with 50 turns, $4 budget, and correct --allowedTools"
    - "run-forecast-validation-test.sh checks both frozen files (prepare.py and forecast.py)"
    - "run-forecast-validation-test.sh asserts beats_seasonal_naive from json_output"
    - "run-forecast-validation-test.sh counts experiments in results.tsv for EVAL-02"
    - "FINDINGS.md documents baseline scores, best MAPE, experiment count, and frozen file compliance"
  artifacts:
    - path: "tests/fixtures/quarterly_revenue.csv"
      provides: "40-quarter synthetic revenue dataset"
      contains: "quarter,revenue"
    - path: "scripts/run-forecast-validation-test.sh"
      provides: "E2E validation harness for forecasting loop"
      min_lines: 150
    - path: "tests/test_phase14_validation.py"
      provides: "Smoke tests for fixture and harness script"
      min_lines: 40
    - path: ".planning/phases/14-e2e-validation/FINDINGS.md"
      provides: "Documented results of validation run"
      contains: "EVAL-01"
  key_links:
    - from: "scripts/run-forecast-validation-test.sh"
      to: "tests/fixtures/quarterly_revenue.csv"
      via: "copies CSV to project root before scaffold"
      pattern: "quarterly_revenue.csv"
    - from: "scripts/run-forecast-validation-test.sh"
      to: "uv run automl"
      via: "--date-column quarter flag"
      pattern: "date-column quarter"
    - from: "scripts/run-forecast-validation-test.sh"
      to: "scripts/parse_run_result.py"
      via: "post-run diagnostics"
      pattern: "parse_run_result.py"
---

<objective>
Run the full v2.0 forecasting loop end-to-end on synthetic quarterly revenue data and document results in FINDINGS.md.

Purpose: Validate that Phases 11-13 (forecasting infrastructure, template, scaffold/CLI) work together as a complete system -- the agent scaffolds correctly, runs Optuna-tuned models with walk-forward validation, respects frozen files, and beats seasonal naive.
Output: Synthetic dataset, validation harness script, smoke tests, and FINDINGS.md with run results.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-e2e-validation/14-RESEARCH.md
@scripts/run-validation-test.sh
@tests/test_phase7_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate dataset, write harness script, write smoke tests</name>
  <files>tests/fixtures/quarterly_revenue.csv, scripts/run-forecast-validation-test.sh, tests/test_phase14_validation.py</files>
  <action>
Three deliverables in this task:

**1. Generate `tests/fixtures/quarterly_revenue.csv`**

Run this Python snippet to create the fixture (deterministic, seed=42):

```python
import numpy as np
import pandas as pd

np.random.seed(42)
n = 40
quarters = pd.date_range("2014-01-01", periods=n, freq="QS")
trend = np.linspace(1_000_000, 2_000_000, n)
seasonal = np.tile([0.85, 1.1, 1.0, 1.05], 10)
noise = np.random.normal(0, 30_000, n)
revenue = trend * seasonal + noise
df = pd.DataFrame({"quarter": quarters, "revenue": revenue})
df.to_csv("tests/fixtures/quarterly_revenue.csv", index=False)
```

Verify: 40 rows, columns are `quarter` and `revenue`, no NaN values.

**2. Write `scripts/run-forecast-validation-test.sh`**

Model after `scripts/run-validation-test.sh` (Phase 7) with these forecasting-specific changes:

- Header comment: Phase 14 E2E Validation for forecasting path
- `EXPERIMENT_DIR="$PROJECT_ROOT/experiment-quarterly_revenue"`
- `DATASET_CSV="$PROJECT_ROOT/tests/fixtures/quarterly_revenue.csv"`
- Pre-flight checks: verify CSV exists with 40 rows, `uv` available, `claude` available, `CLAUDECODE` env guard
- Scaffold command: `uv run automl quarterly_revenue.csv revenue mape --date-column quarter --goal "Forecast quarterly revenue for 40 synthetic quarters (2014-2024). Beat both naive (repeat last) and seasonal-naive (same quarter last year) baselines." --time-budget 120`
- Post-scaffold verification: check `train.py` contains `DATE_COLUMN`, `forecast.py` exists in experiment dir, `program.md` contains MAPE baseline scores
- `uv sync` in experiment dir
- `git init && git add . && git commit -m "initial scaffold"` in experiment dir
- Claude -p invocation (IDENTICAL to Phase 7 except output filename):
  ```
  claude -p "Follow the CLAUDE.md protocol exactly. NEVER STOP until max-turns is reached." \
      --max-turns 50 \
      --max-budget-usd 4.00 \
      --output-format json \
      --allowedTools "Bash(*)" "Edit(*)" "Write(*)" "Read" "Glob" "Grep" \
      2>&1 | tee forecast-validation-run-output.json
  ```
- Post-run diagnostics section:
  - Run `parse_run_result.py forecast-validation-run-output.json`
  - Assert stop_reason is not `tool_use`
  - Check BOTH frozen files unchanged: `git diff HEAD -- prepare.py` AND `git diff HEAD -- forecast.py`
  - Check `json_output` present in `run.log`
  - Extract `beats_naive`, `beats_seasonal_naive`, `metric_value`, and baseline values from last `json_output` line in `run.log` (use Python inline, handle parse failures gracefully with WARN)
  - Count experiments in `results.tsv` (total rows minus header), count keeps (`draft-keep` or `keep` status)
  - Print results.tsv contents, git log --oneline, run.log tail
  - EVAL-01 assertion: report beats_seasonal_naive status
  - EVAL-02 assertion: report experiment count >= 5 and at least 1 keep
  - Note limitation: last json_output may be from a reverted experiment; advise checking results.tsv for best kept MAPE
- Summary section with next steps (return to Claude Code with results)
- Make script executable: `chmod +x scripts/run-forecast-validation-test.sh`

**3. Write `tests/test_phase14_validation.py`**

Follow the pattern from `tests/test_phase7_validation.py`:

Class `TestQuarterlyRevenueFixture`:
- `test_csv_exists` — file exists at `tests/fixtures/quarterly_revenue.csv`
- `test_csv_has_40_rows` — `len(df) == 40`
- `test_csv_has_quarter_column` — `"quarter" in df.columns`
- `test_csv_has_revenue_column` — `"revenue" in df.columns`
- `test_csv_quarter_is_parseable_datetime` — `pd.to_datetime(df["quarter"])` succeeds without errors
- `test_csv_revenue_is_numeric` — `df["revenue"].dtype` is float64

Class `TestForecastValidationHarnessScript`:
- `test_script_exists` — file exists at `scripts/run-forecast-validation-test.sh`
- `test_script_is_executable` — has executable permission bit
- `test_script_passes_bash_syntax_check` — `bash -n` returns 0
- `test_script_uses_max_turns_50` — content contains `max-turns 50`
- `test_script_uses_parse_run_result` — content contains `parse_run_result.py`
- `test_script_references_quarterly_revenue` — content contains `quarterly_revenue`
- `test_script_has_claudecode_guard` — content contains `CLAUDECODE`
- `test_script_uses_date_column_flag` — content contains `--date-column quarter`
- `test_script_checks_forecast_py_frozen` — content contains `forecast.py` in a frozen-file-check context
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_phase14_validation.py -v</automated>
  </verify>
  <done>quarterly_revenue.csv has 40 rows with quarter+revenue columns; run-forecast-validation-test.sh passes bash -n and contains all required sections (scaffold with --date-column, claude -p with --allowedTools, dual frozen file check, EVAL-01/EVAL-02 assertions); all smoke tests pass</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run validation script outside Claude Code</name>
  <files>none</files>
  <action>
User must run the validation script outside of Claude Code because `claude -p` cannot be launched inside another Claude Code session.

What was built: Task 1 created the synthetic dataset (tests/fixtures/quarterly_revenue.csv), validation harness script (scripts/run-forecast-validation-test.sh), and smoke tests. The harness script will scaffold a forecasting experiment, run the autonomous loop for 50 turns, and report whether the agent beats seasonal naive.

Steps for the user:
1. Open a NEW terminal OUTSIDE of Claude Code
2. Navigate to the project root: `cd /home/tlupo/AutoML`
3. Run the validation script: `./scripts/run-forecast-validation-test.sh`
4. Wait for completion (expected: 5-15 minutes, budget capped at $4.00)
5. Return to Claude Code and provide:
   - The automated assertion results (OK/FAIL/WARN lines)
   - Output of parse_run_result.py (stop_reason, num_turns, cost)
   - Contents of results.tsv from the experiment directory
   - The beats_naive / beats_seasonal_naive values from json_output
   - Whether prepare.py and forecast.py remained unchanged
   - Any errors or unexpected behavior observed
  </action>
  <verify>User provides run output containing assertion results, results.tsv contents, and parse_run_result.py output</verify>
  <done>User has run the validation script and provided the output. The run completed (stop_reason is end_turn or max_turns), and results are available for FINDINGS.md population.</done>
</task>

<task type="auto">
  <name>Task 3: Populate FINDINGS.md from run results</name>
  <files>.planning/phases/14-e2e-validation/FINDINGS.md</files>
  <action>
Using the run output provided by the user in Task 2, create `.planning/phases/14-e2e-validation/FINDINGS.md` with the following structure (template from 14-RESEARCH.md):

```markdown
# Phase 14: E2E Forecasting Validation - Findings

**Run date:** {date}
**Script:** scripts/run-forecast-validation-test.sh

## Run Summary

| Field | Value |
|-------|-------|
| stop_reason | {from parse_run_result.py} |
| num_turns | {N} |
| total_cost_usd | ${X.XX} |
| experiments_run | {N} |
| best_mape | {value from results.tsv - lowest MAPE among kept experiments} |
| naive_baseline_mape | {value} |
| seasonal_naive_baseline_mape | {value} |
| beats_naive | {True/False} |
| beats_seasonal_naive | {True/False} |
| frozen_file_compliance | PASSED/FAILED |
| permission_denials | {N} |

## EVAL-01: Beat Seasonal Naive

- [x/] Best model MAPE < seasonal naive MAPE (walk-forward)
- best_mape: {value} | seasonal_naive: {value}
- Notes: {interpretation}

## EVAL-02: 5+ Keep/Revert Cycles

- [x/] At least 5 experiments completed
- [x/] At least 1 keep decision
- total experiments: {N}
- keep decisions: {N}
- Notes: {describe draft + iteration behavior}

## Forecasting Loop Behavior

{Document draft phase, keep/revert cycling, stagnation handling}

## Frozen File Compliance

- [x/] prepare.py unchanged
- [x/] forecast.py unchanged
- Notes: {any hook firing observed}

## Issues Found

| # | Category | Severity | Description |
|---|----------|----------|-------------|
| {issues if any} |
```

Fill in ALL fields from the user-provided output. Use `[ ]` for unmet criteria, `[x]` for met criteria. If EVAL-01 or EVAL-02 is not met, document the gap honestly. Note the limitation about last json_output potentially being from a reverted experiment -- cross-reference with results.tsv for the actual best kept MAPE.

Interpret "holdout MAPE" as "walk-forward MAPE on test folds" per the architecture (see Pitfall 4 in research).
  </action>
  <verify>
    <automated>test -f /home/tlupo/AutoML/.planning/phases/14-e2e-validation/FINDINGS.md && grep -q "EVAL-01" /home/tlupo/AutoML/.planning/phases/14-e2e-validation/FINDINGS.md && grep -q "EVAL-02" /home/tlupo/AutoML/.planning/phases/14-e2e-validation/FINDINGS.md && echo "FINDINGS.md exists with EVAL-01 and EVAL-02 sections"</automated>
  </verify>
  <done>FINDINGS.md contains all required fields populated from actual run data: run summary table, EVAL-01 assessment (best MAPE vs seasonal naive), EVAL-02 assessment (experiment count and keep count), frozen file compliance, and any issues found</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_phase14_validation.py -v` -- all smoke tests pass (fixture + harness)
2. `bash -n scripts/run-forecast-validation-test.sh` -- script syntax valid
3. `python3 -c "import pandas as pd; df=pd.read_csv('tests/fixtures/quarterly_revenue.csv'); assert len(df)==40"` -- fixture correct
4. FINDINGS.md contains EVAL-01 and EVAL-02 assessments with actual run data
</verification>

<success_criteria>
- quarterly_revenue.csv exists with 40 rows, quarter+revenue columns, deterministic (seed=42)
- run-forecast-validation-test.sh is executable, passes bash -n, scaffolds with --date-column, runs claude -p with correct flags, checks both frozen files, asserts EVAL-01 and EVAL-02
- All smoke tests in test_phase14_validation.py pass
- FINDINGS.md documents the actual validation run results with EVAL-01 and EVAL-02 verdicts
</success_criteria>

<output>
After completion, create `.planning/phases/14-e2e-validation/14-01-SUMMARY.md`
</output>
