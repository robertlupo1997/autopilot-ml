---
phase: 16-template-and-protocol-updates
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/automl/templates/claude.md.tmpl
  - src/automl/templates/claude_forecast.md.tmpl
  - tests/test_templates.py
  - tests/test_train_template_forecast.py
autonomous: true
requirements: [KNOW-02, PROT-01, PROT-02, PROT-03]

must_haves:
  truths:
    - "Both templates instruct agent to read experiments.md before each iteration and update it after results"
    - "Both templates instruct agent to run git diff HEAD~1 -- train.py and git log --oneline -5 before each iteration"
    - "Both templates instruct agent to write a Hypothesis section in each commit message"
    - "Both claude.md.tmpl and claude_forecast.md.tmpl carry all v3.0 protocol rules"
  artifacts:
    - path: "src/automl/templates/claude.md.tmpl"
      provides: "v3.0 protocol rules: journal read/write, diff-aware iteration, hypothesis commits"
      contains: "experiments.md"
    - path: "src/automl/templates/claude_forecast.md.tmpl"
      provides: "v3.0 protocol rules: journal read/write, diff-aware iteration, hypothesis commits"
      contains: "experiments.md"
    - path: "tests/test_templates.py"
      provides: "Structural tests for v3.0 protocol rules in claude.md.tmpl"
      contains: "test_journal"
    - path: "tests/test_train_template_forecast.py"
      provides: "Structural tests for v3.0 protocol rules in claude_forecast.md.tmpl"
      contains: "test_journal"
  key_links:
    - from: "src/automl/templates/claude.md.tmpl"
      to: "experiments.md"
      via: "protocol instruction to read before iteration, update after"
      pattern: "experiments\\.md"
    - from: "src/automl/templates/claude_forecast.md.tmpl"
      to: "experiments.md"
      via: "protocol instruction to read before iteration, update after"
      pattern: "experiments\\.md"
    - from: "src/automl/templates/claude.md.tmpl"
      to: "git diff"
      via: "protocol instruction for diff-aware iteration"
      pattern: "git diff HEAD~1"
---

<objective>
Add v3.0 protocol rules to both CLAUDE.md templates: journal read/write (KNOW-02), diff-aware iteration (PROT-01), hypothesis commit messages (PROT-02), and ensure both templates carry all rules (PROT-03).

Purpose: The agent currently improvises each iteration without reviewing what it learned or what it changed. After this plan, the agent reads accumulated knowledge before acting, reviews its recent diffs to avoid repeating changes, and writes testable hypotheses in commit messages -- making each iteration informed rather than random.

Output: Updated claude.md.tmpl and claude_forecast.md.tmpl with protocol rules, structural tests confirming all rules present.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-diagnosis-and-journal-infrastructure/15-02-SUMMARY.md
@.planning/phases/16-template-and-protocol-updates/16-01-SUMMARY.md

<interfaces>
<!-- Current template structures -->

From src/automl/templates/claude.md.tmpl (v1 classification):
- Files section: lists prepare.py, train.py, program.md, results.tsv, run.log
- Phase 2 step 1: "Read program.md"
- Phase 2 step 2: "Review results.tsv"
- Phase 2 step 4: Commit with plain description
- Rules section: 9 rules (NEVER STOP, NEVER modify prepare.py, etc.)

From src/automl/templates/claude_forecast.md.tmpl (v2 forecasting):
- Files section: lists prepare.py, forecast.py, train.py, program.md, results.tsv, run.log
- Phase 2 step 1: "Read program.md"
- Phase 2 step 2: "Review results.tsv"
- Phase 2 step 4: Commit with plain description
- Rules section: 10 rules (includes shift-first, dual-baseline, etc.)
- NOTE: After 16-01 completes, this template will also have experiments.md in Files, rule 11 (DIAG-03 diagnostic patterns), and updated step 6 grepping diagnostic_output.

From experiments.md.tmpl (Phase 15):
- Sections: Dataset Context, What Works, What Doesn't, Hypotheses Queue, Error Patterns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v3.0 protocol rules to claude.md.tmpl (classification)</name>
  <files>src/automl/templates/claude.md.tmpl, tests/test_templates.py</files>
  <action>
    1. Add structural tests to tests/test_templates.py in TestClaudeMdTemplate:
       - test_journal_read_write_rule: assert "experiments.md" in template AND "read" and "update" appear near it (KNOW-02)
       - test_diff_aware_rule: assert "git diff HEAD~1" in template AND "git log --oneline" in template (PROT-01)
       - test_hypothesis_commit_rule: assert "Hypothesis" in template AND "commit message" appears near it (PROT-02)
       - test_experiments_md_in_files_section: assert "experiments.md" appears in the template (PROT-03 coverage)

    2. Update src/automl/templates/claude.md.tmpl:

       a. Add experiments.md to the Files section (after run.log):
          ```
          - `experiments.md` -- Experiment journal. Read before each iteration for accumulated
            knowledge. Update after each experiment with findings. Contains sections: What Works,
            What Doesn't, Hypotheses Queue, Error Patterns.
          ```

       b. Update Phase 2 Iterative Improvement Loop steps. Insert new steps after existing step 1 ("Read program.md"):
          ```
          2. **Read `experiments.md`** for accumulated knowledge from prior iterations. Check
             What Works (don't undo winning changes), What Doesn't (don't repeat failed ideas),
             and Hypotheses Queue (pick your next idea from here if available).
          3. **Review recent changes.** Run:
             ```bash
             git diff HEAD~1 -- train.py
             git log --oneline -5
             ```
             Understand what you changed last iteration and what the trajectory looks like.
             This prevents accidentally undoing a winning change or repeating an approach.
          4. **Review `results.tsv`** history. Think about what to try next based on
             past successes and failures. Avoid repeating failed approaches.
          ```
          Then renumber the remaining steps (old step 3 "Edit train.py" becomes step 5, etc.)

       c. Update the commit step (now step 7, was step 4) to require hypothesis:
          ```
          7. **Commit:** `git add train.py && git commit -m "description of change"` where the
             commit message includes a `## Hypothesis` section:
             ```
             try: add polynomial features for non-linear patterns

             ## Hypothesis
             Adding degree-2 polynomial lag features should capture non-linear revenue
             patterns. Expected to improve metric by ~5% based on residual analysis
             showing quadratic trend in errors.
             ```
          ```

       d. Update the experiments.md update step. After the keep/revert decision (old step 9/10), add:
          ```
          12. **Update `experiments.md`** with findings:
              - If KEEP: add the approach to "What Works" with the score delta.
              - If REVERT: add the approach to "What Doesn't" with why it failed.
              - Update "Hypotheses Queue": remove the tested hypothesis, add any new
                ideas suggested by the results.
          ```
          Then renumber remaining steps.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_templates.py -x -q</automated>
  </verify>
  <done>claude.md.tmpl has experiments.md in Files, journal read before iteration, diff-aware review, hypothesis commit format, journal update after results; all structural tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add v3.0 protocol rules to claude_forecast.md.tmpl (forecasting)</name>
  <files>src/automl/templates/claude_forecast.md.tmpl, tests/test_train_template_forecast.py</files>
  <action>
    1. Add structural tests to tests/test_train_template_forecast.py in TestClaudeForecastTemplate:
       - test_journal_read_write_rule: assert "experiments.md" in template AND "read" and "update" appear near it (KNOW-02)
       - test_diff_aware_rule: assert "git diff HEAD~1" in template AND "git log --oneline" in template (PROT-01)
       - test_hypothesis_commit_rule: assert "Hypothesis" in template AND "commit message" (PROT-02)
       - test_experiments_md_in_files_section: assert "experiments.md" appears in template (PROT-03 coverage)

    2. Update src/automl/templates/claude_forecast.md.tmpl with the SAME v3.0 protocol additions as Task 1, adapted for forecasting context:

       a. Add experiments.md to the Files section (after run.log) IF NOT ALREADY PRESENT from 16-01.
          Same text as classification template. 16-01 already added experiments.md to the Files section
          and added rule 11 (DIAG-03). Check the current state of the file first -- do not duplicate
          entries that 16-01 already added.

       b. Update Phase 2 steps -- insert same new steps 2, 3 after existing step 1:
          - Step 2: Read experiments.md
          - Step 3: Review recent changes (git diff HEAD~1 -- train.py, git log --oneline -5)
          - Step 4: Review results.tsv (existing, renumbered)
          Then renumber remaining steps.

       c. Update commit step to require hypothesis section (same format as classification).

       d. Add experiments.md update step after keep/revert (same as classification):
          - KEEP: add to What Works with MAPE delta
          - REVERT: add to What Doesn't
          - Update Hypotheses Queue

       IMPORTANT: Do not disturb existing forecast-specific rules (dual-baseline gate, shift-first, MAPE direction, no CV loop, feature cap, trial budget) or 16-01's additions (rule 11 DIAG-03, diagnostic_output in step 6). Only ADD the v3.0 protocol steps to the iteration loop and rules.
  </action>
  <verify>
    <automated>cd /home/tlupo/AutoML && uv run pytest tests/test_train_template_forecast.py tests/test_templates.py -x -q</automated>
  </verify>
  <done>claude_forecast.md.tmpl has all v3.0 protocol rules matching classification template; both templates carry journal read/write, diff-aware iteration, and hypothesis commits; all tests pass</done>
</task>

</tasks>

<verification>
cd /home/tlupo/AutoML && uv run pytest tests/test_templates.py tests/test_train_template_forecast.py tests/test_scaffold.py -x -q
</verification>

<success_criteria>
- Both templates list experiments.md in their Files section
- Both templates instruct reading experiments.md before each iteration (KNOW-02)
- Both templates instruct git diff HEAD~1 -- train.py and git log --oneline -5 (PROT-01)
- Both templates require Hypothesis section in commit messages (PROT-02)
- Both templates instruct updating experiments.md after keep/revert (KNOW-02)
- All existing template tests still pass (no regressions)
- New v3.0 protocol structural tests pass
- PROT-03 verified by both templates carrying all rules
</success_criteria>

<output>
After completion, create `.planning/phases/16-template-and-protocol-updates/16-02-SUMMARY.md`
</output>
