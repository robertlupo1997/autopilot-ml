---
phase: 16-wire-template-runtime-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlforge/templates/tabular_train.py.j2
  - src/mlforge/tabular/__init__.py
  - src/mlforge/templates/base_claude.md.j2
  - tests/mlforge/test_templates.py
autonomous: true
requirements: [INTL-03, UX-03]

must_haves:
  truths:
    - "Rendered tabular train.py writes predictions.csv with y_true and y_pred columns after best trial"
    - "Rendered tabular train.py saves best_model.joblib via joblib.dump after best trial"
    - "CLAUDE.md template instructs agent to preserve predictions.csv and best_model.joblib writes"
    - "_run_diagnostics() can find and process predictions.csv produced by the template"
    - "export_artifact() can find best_model.joblib produced by the template"
  artifacts:
    - path: "src/mlforge/templates/tabular_train.py.j2"
      provides: "Predictions CSV + model joblib writes"
      contains: "predictions.csv"
    - path: "src/mlforge/tabular/__init__.py"
      provides: "Artifact preservation domain rule"
      contains: "predictions.csv"
    - path: "tests/mlforge/test_templates.py"
      provides: "Template artifact tests"
      contains: "predictions"
  key_links:
    - from: "src/mlforge/templates/tabular_train.py.j2"
      to: "src/mlforge/engine.py:_run_diagnostics"
      via: "predictions.csv file with y_true/y_pred columns"
      pattern: "y_true.*y_pred"
    - from: "src/mlforge/templates/tabular_train.py.j2"
      to: "src/mlforge/export.py:export_artifact"
      via: "best_model.joblib file"
      pattern: "best_model\\.joblib"
---

<objective>
Add predictions.csv and best_model.joblib writes to the tabular train template, and add a CLAUDE.md domain rule preserving these artifact writes.

Purpose: Close INT-01 (predictions.csv never written) and INT-02 (model file never saved) so diagnostics engine and artifact export actually fire at runtime.
Output: Updated tabular_train.py.j2, TabularPlugin domain rule, and template tests.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wire-template-runtime-artifacts/16-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs -->

From src/mlforge/engine.py (lines 420-428) -- predictions.csv consumer:
```python
predictions_path = self.experiment_dir / "predictions.csv"
if not predictions_path.exists():
    return
df = pd.read_csv(predictions_path)
y_true = df["y_true"].values
y_pred = df["y_pred"].values
```

From src/mlforge/export.py (lines 35-36) -- model file consumer:
```python
model_path = experiment_dir / "best_model.joblib"
if not model_path.exists():
    return None
```

From src/mlforge/tabular/__init__.py -- template_context() returns:
```python
{"domain_rules": rules, "extra_sections": []}
```

From src/mlforge/templates/base_claude.md.j2 -- domain_rules rendering:
```jinja2
## Domain Rules
{% for rule in domain_rules %}
{{ loop.index }}. {{ rule }}
{% endfor %}
```

Current tabular_train.py.j2 structure:
- Lines 1-32: imports + config constants
- Lines 45-92: objective() function (loads data, builds preprocessor, trains, returns metric)
- Lines 95-99: __main__ block (create study, optimize, print JSON)

CRITICAL: Data loading/splitting is inside objective() scope. Must restructure so data is accessible in __main__ for post-study predictions and model save.
CRITICAL: Column names MUST be y_true and y_pred (not actual/predicted) -- engine.py hardcodes these.
CRITICAL: JSON print MUST remain the LAST output line -- engine parses it.

Existing test pattern (tests/mlforge/test_templates.py):
```python
env = get_template_env()
template = env.get_template("tabular_train.py.j2")
output = template.render(csv_path="data.csv", target_column="target", metric="accuracy", time_budget=5, task="classification", date_column=None)
assert "json.dumps" in output
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add artifact write tests and implement template changes</name>
  <files>tests/mlforge/test_templates.py, src/mlforge/templates/tabular_train.py.j2</files>
  <behavior>
    - Test: Rendered classification template contains `import joblib`
    - Test: Rendered classification template contains `predictions.csv` write with `y_true` and `y_pred` column names
    - Test: Rendered classification template contains `joblib.dump` and `best_model.joblib`
    - Test: Rendered regression template contains `predictions.csv` write with `y_true` and `y_pred` column names
    - Test: Rendered regression template contains `joblib.dump` and `best_model.joblib`
    - Test: JSON output (`json.dumps`) appears AFTER artifact writes in the rendered template (last line of __main__)
  </behavior>
  <action>
RED: Add a new test class `TestTabularTrainArtifacts` in `tests/mlforge/test_templates.py`. Tests render the tabular_train.py.j2 template for both classification and regression tasks and assert:
1. `import joblib` present in rendered output
2. `import pandas as pd` present (or `pd` import for predictions CSV)
3. String `"y_true"` and `"y_pred"` appear in a `pd.DataFrame` or `to_csv` context
4. String `predictions.csv` appears in rendered output
5. String `best_model.joblib` appears in rendered output
6. String `joblib.dump` appears in rendered output
7. The `json.dumps` call appears after `predictions.csv` and `joblib.dump` (verify by checking string index positions)

GREEN: Modify `src/mlforge/templates/tabular_train.py.j2`:
1. Add `import joblib` and `import pandas as pd` to the imports section at the top
2. Restructure `__main__` block: after `study.optimize()`, add a post-study section that:
   a. Extracts `study.best_params`
   b. Reloads data: `df = load_data(Path(CSV_PATH))` then `X_train, X_test, y_train, y_test = split_data(df, TARGET_COLUMN)`
   c. Builds preprocessor: `preprocessor = build_preprocessor(X_train)`, fits and transforms
   d. Reconstructs the best model using an if/elif chain on `best_params["model"]` (same model families as in objective() -- rf/ridge for regression, rf/lr for classification, using Jinja2 `{% if task == "regression" %}` blocks)
   e. Fits `best_model.fit(X_train_processed, y_train)`
   f. Generates predictions: `preds = best_model.predict(X_test_processed)`
   g. Writes predictions CSV: `pd.DataFrame({"y_true": y_test, "y_pred": preds}).to_csv("predictions.csv", index=False)`
   h. Saves model: `joblib.dump(best_model, "best_model.joblib")`
3. Keep the `print(json.dumps(...))` call as the VERY LAST line of __main__ (after artifact writes)
4. Do NOT change the objective() function -- it remains as-is for Optuna trials

Note: Data loading is repeated (once in objective, once in __main__). This is intentional -- objective() runs many trials so it needs its own scope. The __main__ retrain is a single run after the study completes.
  </action>
  <verify>
    <automated>python3 -m pytest tests/mlforge/test_templates.py::TestTabularTrainArtifacts -x -q</automated>
  </verify>
  <done>Rendered tabular train.py contains predictions.csv write with y_true/y_pred columns, joblib.dump for best_model.joblib, and JSON output remains last line. Both classification and regression variants tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLAUDE.md artifact preservation rule</name>
  <files>src/mlforge/tabular/__init__.py, tests/mlforge/test_templates.py</files>
  <behavior>
    - Test: TabularPlugin.template_context() domain_rules list contains a rule mentioning predictions.csv and best_model.joblib preservation
    - Test: Rendered CLAUDE.md (via render_claude_md) contains artifact preservation instruction
  </behavior>
  <action>
RED: Add tests to `tests/mlforge/test_templates.py`:
1. In a new test class `TestClaudeMdArtifactRule` (or append to existing `TestClaudeMdOutputFormat`):
   - Test that `render_claude_md(plugin, config)` output contains "predictions.csv"
   - Test that `render_claude_md(plugin, config)` output contains "best_model.joblib"
   - Test that the output contains text about "diagnostics" or "export" (the WHY)
   Use the existing MockTabularPlugin pattern from the file (import from test_plugins).

GREEN: Modify `src/mlforge/tabular/__init__.py` in the `template_context()` method:
1. Add one rule to the `rules` list: `"Do NOT remove predictions.csv or best_model.joblib writes from train.py -- diagnostics and artifact export depend on these files"`
2. Place it after the existing rules, before the task-specific CV rule

This is a one-line addition to the rules list. The existing base_claude.md.j2 template already renders domain_rules as a numbered list, so no template changes needed.
  </action>
  <verify>
    <automated>python3 -m pytest tests/mlforge/test_templates.py::TestClaudeMdArtifactRule -x -q && python3 -m pytest tests/mlforge/test_templates.py -x -q</automated>
  </verify>
  <done>CLAUDE.md rendered output includes rule preserving predictions.csv and best_model.joblib with explanation that diagnostics and export depend on them. All existing template tests still pass.</done>
</task>

</tasks>

<verification>
Full test suite passes:
```bash
python3 -m pytest tests/mlforge/test_templates.py -x -q
python3 -m pytest tests/ -x -q
```

Manual spot-check (optional): Render template and inspect artifact write section appears before JSON output line.
</verification>

<success_criteria>
1. Rendered tabular_train.py.j2 (both classification and regression) contains predictions.csv write with y_true/y_pred columns
2. Rendered tabular_train.py.j2 contains joblib.dump("best_model.joblib") call
3. JSON metric output remains the last line of the rendered script
4. CLAUDE.md domain rules include artifact preservation instruction
5. All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-wire-template-runtime-artifacts/16-01-SUMMARY.md`
</output>
